# 备赛冲刺清单 — 距 10-15 双截止约 4 周

> 依据 `TODO.md` + `TODO_Collector.md` 整理，只留**未完成**项，按"对比赛的价值"重排。
> 今日：2026-09-19 ｜ 目标：三赛同投（国创 / AI+教育 OPC / AIC），材料期 10/5 起。
> 现状：代码工程化 ~85%（架构/测试/部署/文档/CI/运维均 ✓），剩下全是"落地验证 + 演示 + 材料"。

---

## 第一梯队：演示不翻车（必须做，约 1 周）

> 这四项决定评委看到的东西是真是假。全卡在"真实 API / 真网"，代码已就绪。

| # | 任务 | 前置条件 | 验收标准 |
|---|------|---------|---------|
| 1 | **真实向量灌库** `seed_collector.py --embed api` | 阿里 embedding 额度恢复（非欠费） | 40 词条真实向量入库，stats 正常 |
| 2 | **RAG 评测真实重跑** `eval_rag.py --embed api` | 同上 | 定夺 RRF vs 加权融合（mock 结论：加权 0.818 > RRF 0.766） |
| 3 | **quiz 简答 LLM 判分端到端联调** | 仅需 MiniMax key 可用（不依赖 embedding） | 出题→答→判分全链路跑通 |
| 4 | **演示数据 + 预演脚本** | 无 | 1 个完整学科图谱（15-20 节点 + 先修边）+ 2-3 条对话演示路径 |

补充：灌库后顺手做 **Batch 1 端到端对话引用**（挂"自动采集"目录提问 DSA → 回答带 KB 引用），演示时最有说服力。

---

## 第二梯队：评委可问的补强（选做，约 1 周，按性价比排序）

1. **题目渲染成可点选项卡片**（`QuizCard.vue`）—— 演示效果直观，前端已有 `quiz_ready.questions` 字段，工作量小
2. **对话内出题轻微漂移** —— 降 `CHAT_QUIZ_TOP_K` 或只用节点正文（已知瑕疵，改配置即可）
3. **Prompt 笔记优化** —— 教学后主动记笔记到节点（纯提示词层，不动代码）
4. **空图谱一键建图接入对话** —— 需后台任务形态（不能同步跑 loop，超时 60s 限制），工作量大，**时间不够就砍**

**暂缓**（等都做完还有余力再说）：
- OI-wiki 真网联调（已挂"待议"，需用户定时间）
- 嵌入语义去重（依赖嵌入 API 恢复 + 收益未量化）
- 出题改非思考模型 A/B（可选优化）

---

## 明确不做（比赛前）

- **Batch 3/4/v2 采集模块全部**（题库导入 / 通用网页抓取 / 登录站 / 本地 embedding）—— 属于赛后路线图
- **P2 功能路线图全部**（Cross-Encoder 精排 / HyDE / 多角色讨论 / 白板）—— 加分项不是保命项
- **遗留技术债**（quiz_splitter 接入 / 图谱 RAG 双检索 / conversations 与 run 关联）—— 不影响演示，赛后决策

---

## 时间线建议

| 周 | 干什么 |
|----|--------|
| **9/19 - 9/26** | 第一梯队 4 项（灌库→评测→判分联调→演示数据），赶上额度恢复的窗口 |
| **9/26 - 10/5** | 第二梯队按序做 + 材料撰写启动（技术报告引用真实评测数据，材料期 10/5 起） |
| **10/5 - 10/15** | 冲材料 + 演示彩排（跑预演脚本），代码冻结只修 bug |

---

## 风险与依赖（唯一硬风险）

- **阿里 DASHSCOPE embedding 额度**：第一梯队 1、2 两项全卡它。额度一天不恢复，演示数据就是 mock 哈希向量 → 检索质量不可信。**建议立刻确认额度状态**；若短期无法恢复，方案 = mock 演示 + 报告如实标注口径（README 已有先例写法）。
- MiniMax key 独立可用，第 3 项不受 embedding 影响，可先做。

---

## 完成判定口径

- 每完成一项：勾掉 `TODO.md` 对应项，**搬到 `done.md`（只追加）**
- 代码改动回归：`backend/venv/Scripts/python.exe -m pytest backend/tests -q -m "not llm_api"` → 期望 711 passed, 7 deselected
- 材料数字一律用真实重跑结果，不用 mock 结论充数

---

# 怎么做 — 借助 GitHub + AI 的实操流程

> 原则：**GitHub 管任务与证据，AI 干活，你只做决策和验收**。本仓库已有多 AI 会话并发写的先例，纪律比工具重要。

## 一、先把任务挂到 GitHub（半天）

不要靠 TODO.md 的记忆推进，把它变成可追踪的 Issue：

```
仓库 → Issues → 每个第一梯队任务开一个 Issue：
#41 真实向量灌库（seed_collector --embed api）
#42 RAG 评测真实重跑（eval_rag --embed api，定夺 RRF vs 加权）
#43 quiz 简答 LLM 判分端到端联调
#44 演示学科图谱 + 2-3 条预演对话路径
```

- Issue 模板里写清**验收标准**（直接抄本文第一梯队表格的"验收标准"列）
- 打标签：`P0` / `演示` / `blocked-embedding`（前两项挂这个，额度恢复当天就能过滤出来开工）
- 每完成一个就 close + 在 `done.md` 追加一条，Issue 链接贴进 done.md（材料期直接翻 Issue 当证据链）
- 可选：开一个 **Project 看板**（To do / Doing / Done 三列），双截止冲刺用看板比列表直观

## 二、给 AI 派活的正确姿势（每天开工方式）

本项目 AGENTS.md 已定义纪律，你只需要按场景下指令：

| 你要做的事 | 给 AI 的指令模板 | 为什么 |
|---|---|---|
| 跑第一梯队某项 | 「读 AGENTS.md，执行 TODO #43：quiz 简答判分端到端联调，先跑通再贴输出」 | AGENTS.md 会自动注入环境命令与硬约束，不用你复述 |
| 改代码 | 小改直接说；数据层/并发路径要求「先写失败测试再实现」（TDD） | 项目纪律，AI 不会跳步 |
| 修 bug | 「用 systematic-debugging 流程，先定位根因再改，禁止症状补丁」 | 防止越修越坏 |
| 验收 | 「跑 `pytest backend/tests -q -m "not llm_api"`，贴完整输出，711 passed 才算完」 | 证据先于断言（verification-before-completion） |

**开工模板（可直接粘贴）**：

> 读 AGENTS.md 和 `冲刺计划_备赛四周末.md`。本次任务：Issue #N（xxx）。
> 验收标准：……。完成后跑回归测试贴输出，并把完成项从 TODO.md 搬到 done.md（只追加）。

## 三、Git 纪律（与 AI 并发写共存）

本仓库**常有多个 AI 会话同时写文件**，所以：

1. **提交必须路径限定**：`git add backend/app/core/xxx.py backend/tests/test_xxx.py`，**永远不 `git add -A` / `commit -a`**——会把别的会话的半成品卷进去
2. 提交前先 `git status` + 看文件 mtime，内容与预期不符 = 别人的 WIP，绕开
3. **测试与其修复同一提交**；按文件族拆提交，不混无关改动
4. 不主动 push；push 前注意代理：`$env:HTTPS_PROXY="http://127.0.0.1:7897"`
5. 装依赖前 `$env:NO_PROXY="*"`（防注册表代理残留导致 ProxyError）
6. 大改动走 **feature 分支**（如 `feat/quiz-card`），演示主线 `main` 冻结期只收 bugfix PR

## 四、四周节奏（每周末一个 GitHub 检查点）

| 时间 | 动作 | GitHub 留痕 |
|------|------|------------|
| **9/19-9/20** | 开 Issues + 看板；确认 DASHSCOPE 额度；额度没恢复就先让 AI 做 #43（不依赖 embedding） | Issues 全建好 |
| **9/21-9/26** | 第一梯队逐项歼灭：AI 执行 → 你验收 → 路径限定 commit → close Issue | 4 个 Issue 全 close，done.md +4 条 |
| **9/27-10/5** | 第二梯队（QuizCard 优先）+ 启动材料；技术报告引用 Issue 链接与真实评测输出 | `feat/quiz-card` 合入；报告初稿进仓库 |
| **10/6-10/15** | 代码冻结，只修 P0 bug；跑预演脚本彩排，录演示视频 | 冻结声明 + tag `v1.0-competition` |

## 五、AI 用得省又准的三个技巧

1. **一次一个 Issue**：不要一条指令塞四个任务——并发会话 + 大上下文 = 漂移，单 Issue 派活验收最快
2. **让 AI 自查而不是你查**：指令末尾加「完成后跑回归测试并贴输出，若非 711 passed 继续修」——把验证闭环交给 AI，你只看最后一行
3. **所有"结论"要求可复现命令**：评测数字、灌库 stats、判分结果，让 AI 把**命令 + 原始输出**贴进 Issue 评论——材料期写技术报告时直接引用，不用重跑

## 六、如果 embedding 额度迟迟不恢复（Plan B）

1. 开 Issue 记录 blocked 状态与时间线（材料里如实呈现"已知风险与应对"，评委反而加分）
2. 让 AI 用 mock 口径先完成演示数据与预演脚本（#44 不依赖真向量）
3. 材料中沿用 README 既有的"mock 仅替换网络调用，链路逻辑全真实"口径，标注评测为 mock 基线
