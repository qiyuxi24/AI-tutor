# 递归终止条件

## 基本定义
递归终止条件（Base Case）是递归函数中不再进行递归调用的判断条件。它是递归结束的出口，确保递归能够最终停止并返回结果。

## 核心特征
- **直接返回**：满足条件时直接返回结果，不再递归
- **最小问题**：对应问题规模最小的可解情况
- **必须有**：每个递归函数至少需要一个终止条件
- **最先判断**：通常在函数开头判断终止条件

## 常见错误
- 完全忘记编写终止条件
- 终止条件永远无法满足（如判断条件错误）
- 终止条件覆盖不完整，遗漏边界情况