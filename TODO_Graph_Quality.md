# TODO：知识图谱生成质量（粒度 / 去重 / 质控）

> 由来：2026-09-20 对用户 5 的书籍建图结果做了一次全量审计，用户判断"问题很大"，要求先调研三件事 ——
> ① 单个节点该多深 ② 重复生成怎么办 ③ 如何保证生成质量。本文件是这轮调研的结论 + 待办清单。
> 定位：**活清单**（与 `TODO_Context.md` 同级），只列未完成项；做完的搬 `done.md`。
> 相关 SSOT：`docs/知识图谱/知识图谱_参照系契约.md`（粒度口径）、`backend/app/core/kb/graph_generator.py`（生成实现）。

---

## 0. 实测基线（审计对象：`data/knowledge/knowledge.db`，只读，未改数据）

主图 = **user 5**（311 节点 / 659 边，2026-09-20 由 2 个文件共 198,448 字符生成，强化学习主题）。

| 指标 | 实测 | 目标 | 判定 |
|---|---|---|---|
| 节点 / 边 | 311 / 659 | 100–150 / — | ⚠ 过碎（≈640 字符/节点） |
| 正文长度 | 中位 **241 字**，p75=311 | 中位 ≥600 | ⚠ 过浅 |
| 正文 <200 字占比 | **25%**（79/311） | ≤10% | ⚠ 空壳 |
| 同名重复节点 | **20 组**（uid5 内 18 组） | 0 | 🔴 P0 |
| 孤立节点 | 10（uid5） | ≤3% | 一般 |
| 出度 0（叶子） | 75 | — | 偏多 |
| 悬空边 / 自环 / 重复边 / prerequisite 环 | 0 / 0 / 0 / 0 | 0 | ✅ 但由代码约束兜底，非模型之功 |
| 主题污染（非本学科节点） | 9（KMP、最小生成树、LLM 幻觉、小学数学、英语阅读…） | 0 | ⚠ |
| `confidence` 有值 | 11/348 | 100% | ⚠ 建图不写置信度 |
| `board` 非空 | 11/348 | 按书区分 | ⚠ 无法隔离来源 |
| `mastery` = 0 | 337/348 | — | 正常（无学习行为） |

**做得好的（别改坏）**：正文带具体数值（γ≈0.9、π(学习|s)=0.5、-20.00、4×4 格子撞墙规则）；`summary` 324/348 非空且**无一条是"复述名字"**；层级标签 一级84/二级142/三级78；`confusion` 边 9 条质量高（UCB ~ Softmax「易混淆：同为探索」）。

### 0.1 脚本体检基线（2026-09-20，GQ-3；**此节口径为准**）

```bash
cd backend && venv/Scripts/python.exe scripts/inspect_graph_quality.py --user 5   # 复现 uid5
cd backend && venv/Scripts/python.exe scripts/inspect_graph_quality.py            # 全库 + 分用户概览
```

| 指标 | uid5（311 节点 / 659 边，平均度 4.24） | 全库（348 节点 / 679 边） |
|---|---|---|
| 正文长度 | 中位 **213** / p75 284 | 中位 216 / p75 292（MD 缺失 3） |
| <200 字（空壳） | 135（**43.4%**） | 148（42.5%） |
| <400 字 | 285（91.6%） | 310（89.1%） |
| 同名组（L1 归一化） | **23 组 / 51 节点** | 25 组 / 55 节点 |
| 高相似对（L2 字符串，不算同名） | 63 对 | 65 对 |
| 结构健康 | 悬空边 0 / 自环 0 / 重复边 0 / 前置环 0 / 孤立 10 / 连通分量 13 | — |
| 拓扑 | 根 39 / 叶 85（不含孤立 **75**） | — |
| 字段 | summary 310（99.7%）/ confidence 5（1.6%）/ board 3（1.0%）；mastery=0 307 | confidence/board 同量级 |
| tags 分布 | 强化学习 302 + 6 个 ≤2 的疑似污染（人工智能 2、编程基础/AI自动化/基础教育/英语/数据结构与算法/数据结构 各 1） | — |

**与 §0 手工审计的口径差异（都是口径，不是数据变了）**：
- **正文长度**：§0 的 241/311 是**含 MD 头**（`# 标题` + `> 来源标注` ≈28 字）的长度；脚本按**纯正文**算 → 213/284。§0 的"<200 字 25%（79/311）"同理是含头口径，纯正文口径是 43.4%。
- **同名组**：§0 手工数 18（uid5），脚本 23 —— 脚本的归一化把 `_review` / `_extended` / 「（复习）」**后缀档**也算同名（正是 GQ-1 要并的那批），再加上「第三节课学习摘要 ×2」这类字面同名但 §0 未计入的组。
- **叶节点**：§0 的 75 = 脚本的 85 **减去 10 个孤立节点**（脚本两个数都报）。
- 脚本是趋势工具：GQ-5~7 改完 prompt 后用同一命令对比"中位/空壳率/同名组"三项即可判断有没有变好。

---

## 1. Q1：单个节点应该多深？

### 1.1 现在为什么这么浅（根因，不是模型能力问题）

1. **prompt 自己写了上限**：`GRAPH_GENERATOR_SYSTEM_PROMPT` 第 101 行 —— "每次最多提取 8 个知识点；每个 content **控制在 300 字以内**……不要展开长篇示例与推导"。中位 241 字是这个约束的直接产物。
2. **输入块太小**：`GRAPH_CHUNK_CHARS=3000`，但 `chunk_text` 按标题/段落边界切，实测平均只有 **570 字符/块** —— 模型看到的上下文根本撑不起深度内容。
3. **节点数指标挤占深度预算**：要求一次吐 8 个节点 + 300 字 × 8 = 输出被"广度"占满，深度只能让位。

### 1.2 判定"够深"的三条判据（用途倒推）

节点在本项目里同时是**三种单元**，深度必须满足三者中最严的那个：

| 用途 | 对深度的要求 | 出处 |
|---|---|---|
| 出题单元（`quiz_generate` / `grade_answer`） | 必须含**可命题的事实**：定义 + 关键性质 + 至少一个例子或数值 + 易错点。只有一两句定义 → 出不了题或出成"背书题" | `core/quiz/` |
| 讲解单元（对话导学） | 要能独立支撑一次讲解，不靠上下文补全 | `system_prompt_common.j2` |
| 检索单元（RAG） | 语义自足，单独一段能回答一个问题 | `core/rag/`、`core/kb/` |

**可操作判据（写入 prompt 与质检脚本）**：
- **可出题性**：能就这个节点独立出 ≥1 道非背诵题 → 不满足则视为过浅
- **可独立讲解性**：不引用"上文/本节/如前所述"也能读懂
- **时间锚**：`estimated_minutes` 落在 **5–25 分钟**（当前默认 15，但与 241 字明显不匹配）
- **非碎片性**：不得是「章节摘要 / 回顾 / 复习 / 小结」类（当前有 `course_title_class2/3`、`*_review`、`*_summary`、`*_extended` 等 10+ 个）

教育界经验值（参照）：一门课的知识点数量**上限≈教材目录条目数、下限≈章节数**；过细会造成学生畏难、图谱眼花缭乱。本项目 311 节点对应一本书的两个文件，明显偏向"过细"。

### 1.3 建议目标值

| 项 | 当前 | 建议 |
|---|---|---|
| `content` 长度 | 中位 241（硬限 300） | **目标 600–1000 字**，下限 400（<400 判为空壳，触发补写） |
| content 结构 | 自由段落 | **五段式**：定义（1 段）→ 核心要点（3–5 条）→ 典型示例（**必须含具体数值或代码**）→ 易错/易混淆 → 与前后置节点的关系 |
| 每次提取节点数 | 8 | **4–5**（预算让给深度） |
| 单次块字符 | 上限 3000，实测均 570 | **提高有效填充率到 2000+**（放宽切分边界/合并小段） |
| 节点总规模 | 311 | **100–150**（≈ 每 1300–2000 字符 1 个节点） |
| `GRAPH_MAX_TOKENS` | 8000 | 随深度上调（先实测峰值再定，别拍脑袋） |

⚠ **成本对冲**：块变大 + 深度变大 → 单次 token 涨，但块数从 335 降到 ~80–100，"已有节点清单"的 O(n²) 固定开销反而**大幅下降**（清单注入是上次成本审计的第一大头）。净成本大概率持平甚至下降，需实测验证。

---

## 2. Q2：为什么会重复生成？

### 2.1 根因链（已定位到硬证据）

代码里**其实做了**语义去重（嵌入粗筛 0.78 → LLM 判 merge/keep），但仍然漏了 18 组，原因是四层漏洞叠加：

1. 🔴 **嵌入服务欠费 → 去重整条链路静默失效**（硬证据，实测）：
   `text-embedding-v4` 返回 `400 Arrearage`（账号欠费）→ `ApiEmbedder.embed()` 返回空列表 → `_find_dedup_candidates` 直接 `return {}` → **零候选、零合并，且不报错、不告警**。
2. 🔴 **缺"精确同名"这一档**：Neo4j GraphRAG 的 Entity Resolution 是**三档串联** —— `SinglePropertyExactMatch`（精确属性匹配，**零误判、默认档**）→ 语义匹配（阈值 0.85–0.95）→ 模糊匹配。我们**只有中间那档**，而漏掉的 18 组恰恰是**精确同名**（"泡吧问题的奖励函数" ×2、`bar_reward_function` vs `bar_reward_function_extended`）—— 这一档不需要嵌入、不需要 LLM，字符串比对即可全歼。
3. 🟠 **只比"新 ↔ 已有"，不比"批次内新 ↔ 新"**：同一 chunk 一次输出的 4–8 个节点之间互为同义时无人检查。
4. 🟠 **写入层无护栏**：`KnowledgeGraph.add_node` 只校验 `id` 唯一，**不做同名检查**；精确同名并轨只存在于 `knowledge_writer._find_same_name`（Agent 写路径，2026-09-15 已做），**书籍建图走 `create_node_with_content` 绕过了它**。
5. 🟡 判定策略保守（"宁可不合并"），且降级阈值 0.90 只在 **LLM 失败**时启用 —— **嵌入失败根本没有降级路径**。

### 2.2 修复方案：三层护栏（从最严到最松，业界共识顺序）

| 层 | 落点 | 作用 | 依赖嵌入？ |
|---|---|---|---|
| L1 精确同名 | `KnowledgeGraph.create_node_with_content` / `add_node`（**写入层，所有写路径共享**） | name 规范化（去空格/全半角/括号后缀）后完全相同 → 合并到已有节点，不新建 | 否 |
| L2 字符串模糊 | `graph_generator` 新增 | 编辑距离/包含关系（如去掉 `_extended`、`_review`、`（复习）` 后缀后同名）→ 合并或交 LLM 判 | 否 |
| L3 语义 | 现有 `_find_dedup_candidates` | 嵌入粗筛 + LLM 确认 | 是（**必须先修好嵌入 + 失败告警**） |

配套：
- **批次内去重**：写入前先对新节点两两做 L1/L2。
- **嵌入/LLM 失败不得静默**：`_find_dedup_candidates` 拿不到向量时 **warn + 计入 aggregate 返回**，让前端能看到"本次建图去重未生效"。
- **建图后体检**（见 §3.3）：自动跑一遍重复检测并给出合并建议。

---

## 3. Q3：如何保证生成质量？

四层，当前**只有第 1 层的一半**，第 3、4 层基本空白。

### 3.1 生成前（约束）
- prompt 写清粒度判据（可出题 / 可独立讲解 / estimated_minutes 5–25 / 禁输出"摘要·回顾·小结"类节点）
- content 强制五段式模板 + 正例/负例各一个
- id 命名规范（已有，仅 `123` 一个违规）

### 3.2 生成中（护栏，写入层强制）
- 三层去重（§2.2）
- **内容下限校验**：`content` < 400 字 → 拒绝落库并要求重写（或标记 `confidence` 低分）
- **噪声节点拦截**：`未识别内容片段` 这类兜底产物不入库
- **主题一致性**：节点 tags 与学科不符时标记待审（解决 9 个污染节点）
- **写 `confidence`**：建图路径目前完全不写（11/348 有值），导致无法按质量过滤

### 3.3 生成后（体检 —— 当前完全缺失，最该补）
固化本次手工审计为**可重复执行**的质检脚本（建议 `backend/scripts/inspect_graph_quality.py`，只读），输出：
- 规模：节点/边/平均度
- **重复率**：同名组 + 高相似组（L1/L2，不依赖嵌入也可跑）
- **空壳率**：`content` < 400 字占比（按用户/学科分组）
- 结构健康：悬空边、自环、重复边、prerequisite 环、孤立节点、连通分量
- 主题一致性：tags 分布异常值
- 字段完整：summary / confidence / board 填充率
- 拓扑：入度 0 / 出度 0 列表

并可选接一个 `--fix-dupes` 做**自动合并**（保留正文更长者 + 重定向所有边 + 写审计日志），合并前 dry-run 预览。

### 3.4 可观测（当前缺失）
- **token 消耗落盘**：建图走 `call_llm`，token 只 `logger.info` 不落盘 → 上次想查"这次建图花了多少"查不到（只有估算 1.0–1.2M）。复用 `token_counter.extract_usage` 写入 aggregate + 报告文件。
- 每块成功/失败数、跳过数、**合并数** 进 aggregate（合并数目前前端看不到）。

---

## 4. TODO 清单（按优先级）

### P0 — 直接决定图谱可用性

> **GQ-1（写入层精确同名合并）、GQ-2（嵌入失败不得静默）、GQ-3（质检脚本）已于 2026-09-20 落地**，
> 归档在 `done.md` §4.3；判重口径、去重状态语义、脚本用法见该节与本文 §0.1。
> 未决：**欠费**（§5.1）—— 不充值则 L3 语义去重只剩 hash 降级或完全不可用（现在至少不再静默）。

- [ ] **GQ-4 一次性合并已存在的重复节点**（工具已就绪，**待用户确认执行**）
  - 落点：`backend/scripts/inspect_graph_quality.py --fix-dupes`（默认 dry-run）→ 加 `--apply` 执行
  - 保留者规则：先排除**碎片名**节点（`_review`/「（复习）」不当正主，id 更干净），再取正文更长者；
    被删节点的正文若不在保留者里**先并入再删**（不丢内容）
  - 边处理：重定向到保留者；重定向后重复/自环/成前置环的边丢弃（`add_edge` 的 ValueError 即为该情形）
  - 审计：逐条 `logger.info`（落 `backend/logs/tutor.log`）
  - dry-run 实测（2026-09-20）：全库 25 组 / 55 个待删节点，其中 uid5 23 组
  - ⚠ 会**删节点与 MD 文件**（不可回滚）→ 执行前先备份 `data/knowledge/{knowledge.db,nodes/}`；
    也可与 §5.3 的"全量重建 vs 增量修复"一并决策（重建则不必先合并）
  - 验收：重跑体检脚本后同名组 = 0，uid5 节点数 311 → **283**（删 28 个）、全库 348 → 318

### P1 — 深度与粒度

- [x] **GQ-5 解除 300 字硬限，改五段式深度模板** ✅ 2026-09-21（实现完成，真机验证待跑）
  - prompt：content 目标 600–1000 字、下限 400（低于此值**拒收并计数**，`_drop_shallow_nodes`）
  - 五段式（定义/要点/**示例须含具体数值或代码**/易错点/与前后置关系）；每次 8 → **3–5** 个节点
  - 验收：新生成图谱正文**中位 ≥600 字**、空壳率 ≤10% —— ⏳ 待真机小样：`scripts/smoke_graph_depth.py --user-id 5 --node-id 5 --chapters 1 --run --subject 强化学习`
- [x] **GQ-6 提高 chunk 有效填充率** ✅ 2026-09-21
  - 改为 `build_section_tree`（按章节建树）+ `collect_units`（**递归**切成生成单元，下限 1500 / 上限 5000 / 下钻 ≤4 层）
  - 实测（uid5 强化学习 PDF，191,771 字符）：**316 块 / 583 字符 → 43 个单元 / 4402 字符**（最小 1536，无碎片）
  - 顺带修掉两个碎片根因：① `chunk_text` 把代码注释 `# 衰减因子` 当 Markdown 标题反复断块（旧实现 583 字符/块的真正来源）；② 正文折行 `第5章作为整个内容的核心…` 被误判成章标题
  - 验收：块数下降 ≥50% ✅（-86%），且不再有 <1500 字符的碎片单元
  - ⏳ 尚未做：`existing_nodes` 全量注入的 O(n²) 收敛（缺口 #4）；真实 token 量对比需真机跑一次
- [ ] **GQ-7 禁止碎片类节点**：prompt 明确不输出"章节摘要/回顾/复习/小结"；入库侧对这类 name 做拦截或降级为 relation
- [ ] **GQ-8 建图写 `confidence`**：给每个节点打 0–1 置信度，低分节点不参与注入/出题
- [ ] **GQ-9 主题隔离**：启用 `board`（当前 337/348 为空）区分书籍来源；跨学科节点标记待审，杜绝 KMP/最小生成树混入强化学习图

### P2 — 可观测与长期

- [ ] **GQ-10 建图 token 消耗落盘**（复用 `token_counter.extract_usage`，写 aggregate + 报告文件）
- [ ] **GQ-11 aggregate 暴露合并数 / 跳过数 / 失败块数**给前端
- [ ] **GQ-12 清理历史噪声**：`123`（29 字手工占位）、`未识别内容片段`、u1 的 `test_*` 测试节点（7 个）
- [ ] **GQ-13 深度 A/B 对照**：同一本书跑"浅（现状）" vs"深（GQ-5）"，用 `docs/知识图谱/知识图谱_P1扩跳与AB对照实验.md` 的口径比较出题质量与检索命中率

---

## 5. 待确认（需用户拍板）

1. **是否充值阿里 embedding**？不充 → L3 语义去重、kb 混合检索、RAG 向量检索长期降级，只能靠 L1/L2（字符串层）兜底。
   **2026-09-20 复测仍是欠费**：`Error code: 400 … 'type': 'Arrearage'`（`ApiEmbedder.embed()` → `EMBED_EMPTY`）。
   含义：GQ-1 落地后**字面同名**已被挡住，但「栈/堆栈」这类**同义不同名**仍会各建一个节点
   —— 区别是现在会带 `dedup_status: unavailable` 出来（不再静默）。
2. **深度 vs 成本的取舍**：GQ-5/6 会改变单次建图耗时与 token 量，是否接受"一次建图更慢但质量更高"？
3. **是否对已有 311 节点全量重建**（用新 prompt 重跑）还是只做增量修复（GQ-4 合并 + 补写空壳）？—— 建议先修代码，再挑一本书重建做 A/B（GQ-13），别直接全量重跑。

---

## 6. 参考

- 业界去重三档（精确 → 语义 → 模糊，阈值 0.85–0.95 推荐）：Neo4j `neo4j-graphrag-python` Entity Resolution（`SinglePropertyExactMatchResolver` 为默认档，零误判）
- 教育界粒度经验：知识点数量上限≈教材目录条目数、下限≈章节数；关系数量下限≈思维导图，只保留"有利于迁移学习"的关系
- 本仓库：`docs/知识图谱/知识图谱_参照系契约.md`、`backend/app/core/kb/graph_generator.py`、`docs/项目历程_决策与效果记录.md`
- 落地实现（GQ-1~3）：判重 `core/knowledge_graph.py::normalize_node_name / find_node_by_name`（唯一实现）、
  去重状态 `graph_generator._find_dedup_candidates`、体检与合并 `backend/scripts/inspect_graph_quality.py`；
  回归 `backend/tests/test_node_write_paths.py` + `test_graph_quality_script.py`
