# 2026-06-20

- **学习路径推荐模块完成**：
  - `knowledge_graph.py` 新增 `topological_sort()`（Kahn 算法）、`get_learning_path(target_node_id)`、`get_next_to_learn()` 三个方法
  - `graph_analyzer.py` 新增 `decompose_question()` — LLM 拆解用户问题为知识点依赖树（JSON 格式），返回 nodes + prerequisite edges
  - `knowledge.py` 新增 3 个 API 端点：`POST /knowledge/decompose`（拆解+创建骨架图谱）、`GET /knowledge/learning-path`（拓扑排序路径）、`GET /knowledge/next-to-learn`（下一步推荐）
  - `prompts/system_prompt_recursive.j2` — 递归式教学模式模板：理工科直给风格、禁止反问/苏格拉底式提问、框架约束、[NEXT] 标记推进
  - `prompt_loader.py` 支持 `**extra_kwargs` 传递 `current_node` 和 `knowledge_graph_framework`
  - `schemas.py` 新增 `recursive` 模式 + `DecomposeRequest/Response`、`LearningPathResponse`、`NextToLearnResponse`
  - `chat_service.py` 所有函数签名增加 `current_node` 参数，递归模式时注入框架上下文
  - `system_prompt_common.j2` 加入「框架约束」段：所有讲解必须在图谱框架内，严禁脱离
  - `system_prompt_adaptive.j2` 加入框架约束提醒
  - 前端 `InputArea.vue`、`HomeView.vue`、`chatStore.js`、`api/index.js` 注册 `recursive` 模式 + `currentNode` 状态传递
- **一键安装脚本 + 依赖检查**：
  - 创建 `install.ps1`：自动检测 Python 3.10+ / Node.js 18+ → 创建 venv → pip install → npm install → 验证所有依赖
  - 修复 `backend/requirements.txt`：补充缺失的 `openai>=1.0.0`（llm_client.py 依赖）
  - 创建 `backend/.env.example`：模板文件（README 引用了但之前不存在）
  - 验证 12 个 Python 依赖 + 8 个 Node.js 依赖均通过
