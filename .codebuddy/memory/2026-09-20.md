# 2026-09-20（蒸馏版，2026-09-24 压缩；比赛细节见 `docs/比赛/`）

## 运维后台
- 批量删除 `POST /users/batch-delete`（≤100、去重保序、`not_found` 回填、全不存在 404、空列表 422）；事务顺序沿用单删铁律（delete_user_rows → 审计 → commit → purge）。
- **更正（用户反馈）**："太复杂"指的是删除要重填用户名 → **取消确认词**（单删/批量都只要一次 `ElMessageBox.confirm`），行内删除按钮恢复。测试 37 passed。
- 环境坑：IDE 的 safe-delete shim 会拦住 vite 清空 `frontend-admin/dist` → 验证构建改用 `--outDir` 临时目录。

## AIC 比赛（结论，细节在 `docs/比赛/AIC_*`）
- 定选**赛题5 AI+学科交叉**（主投）；加报排序 **赛题2 AI+软件创新 > 赛题4**（赛题2/3 吃"产品分"、不要求教学试点数据；赛题4 与赛题5 抢同一批证据）。推荐组合 = 5+2（1000 元，材料复用 ~60%）。
- 第七届国奖（官方名单全量统计）：算法应用 2155 / 创新 **1071**（国一 151≈14%）/ 挑战 530 / 主题赛 AI+力学 69；一:二:三 ≈ 15:25:30% → **换赛题不提高获奖率，瓶颈在省赛二等以上**。
- **陕西本届无省级赛区** → 西工大走**区域赛**（025-85778806 / office@aicomp.cn 待确认）。
- 学科界定：评分表"未明确具体学科领域"= 0–4 分 → 口径写「计算机科学与技术（工学）」+ 场景写死「《数据结构》课程学习环节」；**最险写法 = 只写"AI 教育助手"**。「交叉学科」是报名时自选子类标签（非强制绑定）；湛江科技学院"智能+非遗"国二证明技术×人文也算交叉。
- 材料红线：禁校名/Logo/指导教师；视频 3-5 分钟 ≤300MB、方案 PDF ≤10M。
- 五件必办：恢复嵌入 API 余额 / ≥3 人前后测 / 补测试报告 / 申请软著 / 锚定《数据结构》+ ≥30 份问卷。
- 抓取技法：赛道详情页 URL **必须带 `.html`**；列表页用 `curl.exe` + 正则 `tracks/tracks-\d+/\d+`；PDF 用 venv 的 PyMuPDF `fitz`；URL 含 `%XX` 会被 cmd 当变量展开 → 用 `[System.Uri]::EscapeDataString()` 现算。
- 子 agent 经验：纯检索类可派 `kiwi`；**下载+解析 PDF 的统计任务自己做**（第二个跑了 63 步返回空）。

## token 记账 / 日志 / 记录库
- 起因"建图花了多少 token 查不到"：图谱生成走 `call_llm`，token 只 `logger.info` 不落盘，且不走 loop 故无 debug_log；脚本直跑时连 logging 都没配。粗估 **1.0–1.2M token/次**（335 块 × 均 570 字符，"已有节点清单"线性注入 = O(n²) 占约一半输入）。
- 新增 `core/llm/usage.py`：`llm_usage` 表落在**同一个** `agent_runs.db`；三个埋点（loop / call / embed）；`model` 取 `resp.model`（降级后只有响应里回显的是真身）；`call_llm` 新增 `kind`/`user_id`（7 个调用点已传值）。出口 `GET /api/v1/agent/usage`。
- 坑：① `usage.py` 不能模块级 import `agent.store`（循环导入）→ 延迟导入；② 假响应也带 usage → 跑一次全量往真实库塞 73 行 → `conftest.py` 加 autouse fixture 只放行显式 `db_dir`。
- 日志地基：新建 `core/logging_setup.py`（幂等、`LOG_DIR` 相对路径按 **backend 目录**解析、pytest 下跳过文件 handler），`app/__init__.py` **导入即配置** → 18 个脚本零改动拿到日志；`main.py` 删 27 行内联。补黑洞 `rag/manager.index_node`（三个 `return 0` 现在分别记原因）；修 `_analyze_and_apply` 的 `KnowledgeGraph()` 在 try 外（火忘任务异常只进 stderr）。
- 记录库底座 `core/records.py`（`DATA_ROOT` = `backend/app/data`、`connect` / `ensure_columns` / `prune_older_than`），**表不合并**：`agent_runs.db` = agent_runs + llm_usage（长期审计），`debug_log.db` = 调试流水（写量大、可整体关闭）。compose 补挂 `./backend/app/data` 与 `./backend/logs`。加表/加列口径已写进 AGENTS.md。
- 坑：删 `import sqlite3` 但类型标注 `-> sqlite3.Connection` 还在 → 46 个测试收集期 ImportError；`prune(days=30)` 删的是"**早于** 30 天"，窗口内必须留。

## 图谱生成质量（GQ）
- 只读审计（uid5 311 节点 / 659 边）：结构健康靠代码硬约束（自环/枚举/去重/`_has_path`），不是模型生成得好；**P0 = 同名重复 20 组**（uid5 内 18 组，正文相似度仅 0.09–0.44）；P1 正文空壳 25%（中位 241 字）、主题污染 9 个非 RL 节点；`board` 337/348 为空。
- **重复根因链**：① 嵌入 `text-embedding-v4` 返回 400 Arrearage → `ApiEmbedder.embed()` 返空 → 语义去重**静默失效**；② 缺 Neo4j 三档里的第一档"精确同名"（漏的 18 组恰恰全是精确同名）；③ 只比"新↔已有"，不比批次内"新↔新"；④ 建图走 `create_node_with_content`，绕过只在 agent 路径生效的并轨。修法 = L1 精确（下沉写入层）→ L2 字符串模糊（剥 `_extended`/`（复习）`）→ L3 语义（失败必须告警）。
- 深度不足的根因是 **prompt 自限**（"最多 8 个知识点 / 300 字以内"）+ `chunk_text` 只填 570 字符 → 建议 content 600–1000 字、每批 4–5 节点、规模 100–150。
- **GQ-1~3 已落地**：`normalize_node_name()` / `find_node_by_name()`（**判重唯一实现**）/ `create_node_with_content` 命中即并轨且**返回实际落点 ID**；`_dedup_status`（ok / degraded / unavailable）经 aggregate 带出；体检脚本 `backend/scripts/inspect_graph_quality.py`（默认 sqlite 只读，`--user N`、`--fix-dupes [--apply]`）。
- ⚠️ 踩坑：边**按 id 引用** → 并轨后只记 name→id 映射救不了（`stack → queue_v2` 的边被静默丢），需 `node_id_alias`。
- GQ-4 存量合并 dry-run：25 组 / 55 个待删节点（uid5 311→283），**未执行**（会删数据，等确认）。
- 测试基线演进：751 → 756 → 760 → **770 passed, 7 deselected**（AGENTS.md 里的 711 是 09-15 旧值）。
