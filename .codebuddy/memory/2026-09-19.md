# 2026-09-19（蒸馏版，2026-09-24 压缩）

## 运维后台（独立服务 5174 / 8001）
- 首版**根本无法启动**（P0）：`auth.py` 残留 SQLAlchemy 写法与 sqlite3 `db.py` 不匹配（导入即崩）、重置密码用错列名（实际 `password_hash`）、`add_audit_log` 内部 commit 破坏原子性、路由 `async def` 里跑阻塞 sqlite。已全部修：纯 sqlite3 + dataclass/os.getenv（弃 pydantic-settings）+ bcrypt + 同步 `def` 路由 + 审计与业务同事务 + 审计 IP 取 `request.client.host`。
- 主系统内嵌方案下线：删 `backend/app/api/v1/admin.py`、`frontend/src/views/AdminView.vue`、`/admin` 路由与菜单项。
- 管理员 CRUD（晚间补齐）：`require_super_admin()` + 三条自锁（禁自我禁用/降级/删除 + 必留至少一个启用超管 —— 因自我保护在前，"最后超管"守卫在 API 下不可达，改由单元测试直接测守卫函数保持活代码）。
- **删号语义**：`delete_user_rows()`（不 commit）→ 审计 → `commit()` → `purge_user_storage()`（文件删除无法回滚，顺序反了会半残）。
- 前端：`Admins.vue` / `Users.vue` / `AuditLogs.vue`，`api.js` 统一带 token + 401 回登录。
- 测试 `cd backend-admin && ..\backend\venv\Scripts\python.exe -m pytest tests -q` → 13 → **30 passed**。fixture 必须 monkeypatch `backend_data_dir`/`conversations_db` 到 tmp，否则删号用例会 rmtree 真实数据。
- 📌 复盘：交付前**从未跑过服务实例**，IDE lint 全绿不能说明能跑 → 新服务必须跑一次真实启动 + 一条关键路径。

## 主系统存储布局（本日实测确认，旧记忆已更正）
- 图谱/用户 `<root>/data/knowledge/knowledge.db` + `data/knowledge/nodes/{uid}/`（`nodes.file_path` 是装饰性相对串，**不能 unlink**）；对话 `<root>/data/conversations/conversations.db`；kb `backend/data/kb/{uid}/`；图谱向量 `backend/data/rag/{uid}/`。
- ⚠️ 口径后来又被 09-20 修正：agent_runs/debug_log 实际在 `backend/app/data/`（**不在** compose 卷 `./backend/data` 内），09-20 已补挂卷。

## SSH / 远程部署
- 本机事实：`~/.ssh` **已有** `id_ed25519`（旧记忆"无密钥"是错的）；`ssh` 不在 PATH，真身 `C:\Windows\System32\OpenSSH\ssh.exe`；无 plink/sshpass/scp。
- 🔑 **Windows OpenSSH 支持 `SSH_ASKPASS`**（9.5p2 实测）：`.cmd` 脚本可直接当 askpass（`@echo off` + `echo 密码`），配合 `SSH_ASKPASS_REQUIRE=force` → 非交互终端也能做密码认证。已装公钥到远端 → 免密直连 `ssh wojtek@100.90.96.111`。
- 远端画像：Ubuntu 26.04 / i7-13700 / 30G / 盘 354G 可用 / Docker 29.1.3 + Compose 2.40.3 / Node v22 / Python 3.14；内网 `100.90.96.111`（Tailscale）。sudo 无免密 → 每条 `echo pw | sudo -S -k`（**无 tty 时凭据不缓存**）。
- 部署：新增 `deploy/deploy.ps1`（tar 打包含 .env → scp → 解包 `/home/wojtek/ai-tutor` → `docker compose up -d --build` → 轮询 healthy）。访问 `http://100.90.96.111:8080`，容器 `ai-tutor`。⚠️ **tar ≠ rsync**：本地删的文件远端不会删。
- 终端坑：PS 调 exe 必须 `&`；复杂嵌套引号会被包装层搞坏 → 本地写文件再 scp；`${Server}:/tmp` 必须加花括号。

## 评价体系 / AIC 首轮
- 评价体系：用户拍板方案 A（独立 `core/assessment/`），**建设期属其他同学** → 交付 `TODO_Assessment.md`。约束：不动 quiz 出题与 schema、不动 `grade_answer` 对外接口、沿用 `nodes.mastery` 单一数据源。
- AIC：`docs/比赛/AIC_调研_晋级机制与往届获奖项目分析.md`。无直通国赛（校赛 ≤10/10 → 省赛报名 10/15 20:00 → 总决赛 11 月中下旬），**瓶颈在省赛一等**。第六届公式 `((0.5A+0.2B+0.3F)/100)×(0.5C+0.4D+0.1E)+G`（**第八届已作废**，改为每题百分制，见 09-20）。自评差距：现状 ≈50 分（省二三）→ 做完 P0 ≈82 分（国一边缘），**差的 30 分全在证据层**（对照实验、一镜到底视频、完整学科图谱、真实用户）。⚠️ 参照系契约 AC-L1-3/L2-1 未满足前，比赛材料不得宣称"参照系"。
- 8.5 节自我修正：D 完成度打分过高 —— 7 个一级页面平铺、**零演示数据**（评委登录见空系统）、材料是功能清单式而非用户故事式。新增 P0：seed_demo 演示账号 / 叙事重写 / 跑 eval 出表。

## Bug 修复与小模块
- **E-AUTH-005 误报**：`get_current_user_from_token` 把 `log_error` 写在函数第一行 → 成功路径也打警告。移进 except 分支。守卫 `tests/test_auth.py`（4 例）。
- users 表补列：并发会话加了账号状态/role/last_login 但无迁移，且局部变量 `status` 遮蔽 `fastapi.status` → 登录必 500。新增 `ensure_user_columns()`（PRAGMA 幂等 ALTER），三处建表同步。
- 传输加密（RSA 2048）：后端 `core/transport_crypto.py` + `GET /auth/public-key`，私钥 `data/keys/rsa_private.pem`；前端用 **node-forge 而非 Web Crypto**（部署地址 `http://内网IP:8080` 是非安全上下文，`crypto.subtle` 为 undefined）。坑：forge 按 latin1 处理字符串 → 必须先 `encodeUtf8`。11 例测试 + 跨语言冒烟。

## Agent Loop 安全边界（接并发会话未完成部分）
- 诊断：无 `while True`，但有四类退化 —— 调用总数无界 / 无重复参数检测（**Tool Result Clearing 会诱发**重取同一份）/ 连续失败无熔断 / 日志几乎为零。
- `_LoopGuard`（后拆为 `agent/loop_guard.py`）：调用总数 12 / 墙钟 180s / 同参数重复 ≤2 / 连续失败 3；**只判定不执行**；越界回填写明原因的拒绝文案但仍 append tool 消息（保 `tool_call_id` 配对，否则 400）。
- `stop_reason` ∈ natural/max_rounds/time_budget/call_budget/fail_circuit/error，三处同值（结果 / AGENT_DONE / evidence），未动表结构。
- 修真 bug：`chat_quiz._run_guarded` 加 `wait_for(120s)` —— 原先 LLM 卡住会让 `_INFLIGHT` 永不释放 → **该用户此后永远出不了题**。
- 新增 `agent/debug_log.py`：控制台 + SQLite 双写（`backend/app/data/agent_debug/debug_log.db`，14 天，`AGENT_DEBUG_LOG=0` 关闭）；`RunLogger` 绑定 run_id；范围写死（记 run/round/tool/llm/context/bg，**不记正文、提示词全文、工具完整返回、密钥**；截断 2K/8K/单值 500）。
- 测试演进：726 → 731 → **739 passed**（含 llm_api 时有 15/24 失败，全部是并发会话改造 `loop.py` 的中间态，勿据此判回归）。

## 本日教训
- **并发会话改同一文件时报错点会移动**，单文件跑绿而全量报错 → 先比对 mtime，等对方落地再重跑。
