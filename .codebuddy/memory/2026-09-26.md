# 2026-09-26

## 启动脚本收敛（4 → 3，唯一入口）
- 目标（用户定）：只留一个启动入口，内部按「有 Docker → 容器，否则本机」自动降级。
- 落地：
  - `start-docker.ps1` **整段并入 `start.ps1` 后删除**。
  - `start.ps1` 新增 `param(-Local -Docker -Force -Port)`；决策段 `Get-DockerVersion`（`Get-Command docker` + `Invoke-Native { docker info }`，允许失败）判定 `$useDocker`；`-Local`/`-Docker` 互斥，`-Docker` 但守护进程不可用时明确报错。
  - 容器分支保持原逻辑（源码/镜像时间比 → 重建判断 → 健康检查 → 局域网 IP → 防火墙 profile 自检），末尾 `exit 0` 不再落到本机段；**本机逻辑零改动**。
- 保留（按用户决定）：`start.cmd`（2 行双击壳，同一入口）、`install.ps1`（首次安装/CI 独立）。
- 文档同步：README / README.en / CONTRIBUTING / CONTRIBUTING.en / deploy/README / AI-tutor_通俗介绍 / docs/运维部署 两篇 / AGENTS.md §0 新增「一键启动」行。
- 验证：PowerShell AST `SYNTAX OK`；`.\start.ps1 -Local -Docker` → `[ERROR] -Local 与 -Docker 互斥`，rc=1，未启动服务。**未跑完整启动**（会真实拉起服务/容器）。

## 坑 / 注意
- ⚠️ 行为变化：双击 `start.cmd` 时若 Docker Desktop 在运行 → 走容器模式；要固定本机需在 `start.cmd` 加 `-Local`。
- ⚠️ 并发隔离教训：`AGENTS.md` 工作区混有知识图谱会话的 WIP（3 个 hunk）。PowerShell 管道喂 `git add -p` 会带 **BOM**（报 `got 'y'`）导致失败 → 改用「备份文件 → `git checkout` 回 HEAD → 只加本次行 → 提交 → 还原备份」。
- 未触碰：`MEMORY.md` 与 09-13/15/19/20 日记忆的未提交改动（属其他会话）；`docs/比赛/`（冻结）。
