# 2026-08-31

## 标杆项目对标分析（docs/标杆项目对标分析.md）
拉取 DeepTutor(HKUDS 37k star)、adaptive-edu(上海二工大)、ALS v3.2(ydsgangge-ux)、OpenMAIC 四个项目对比 TutorAgent 功能差距。

**结论**：底座（图谱+混合RAG+Agent工具）已强于多数同类，缺的是"学习工具化"表层模块，按优先级：
1. 学习进度仪表盘 + 掌握度可视化（COMPETITION P1 已列，成本低）
2. 间隔复习 SM-2（对标 ALS planning/scheduler，纯 Python 无新依赖，卡片=图谱节点+掌握度）
3. 测评闭环（quiz ↔ 图谱联动，判分回写掌握度）
4. 主动知识缺口检测报告（LLM 基于掌握度+会话历史生成薄弱清单）
5. 复习卡导出、轻量多角色讨论（"AI 同学"）

第三梯队（赛后/论文）：MEFKT 知识追踪、三层记忆架构(L1/L2/L3)、沉浸式阅读、Deep Research、多智能体 subagent。

## 参考资料文档（docs/参考资料_同类Agent项目对标与学习路线.md）
用户要求把调研链接+内容整理成 md 放入参考文档（docs/）。已创建，含：4 个项目的仓库/论文/文章链接 + 内容整理（DeepTutor 四能力块、adaptive-edu MEFKT 闭环、ALS SM-2、OpenMAIC 已落地）+ 每项目"可借鉴点对应本项目"对照表 + 实现路线（第一梯队：仪表盘/SM-2/测评闭环）+ 延伸论文。与 docs/标杆项目对标分析.md 互补：那份是差距结论，这份是一手资料。

## 学习进度仪表盘：调研 + P0 落地（docs/学习进度仪表盘设计.md）
用户要求先调研再实施"学习进度仪表盘 + 知识图谱联动"。核心理念：**图谱=地图=科技树**，进度不是独立统计而是"看图谱就知道到哪了"。

**方案分层**：概览层（DashboardView 独立视图，P1）+ 科技树层（图谱本身四色+路径+脉冲，P0）。数据流：mastery 单一数据源（AI 工具 update_mastery 写入 nodes.mastery）→ SSE graph_updated → 前端实时刷新；仪表盘聚合 /knowledge/stats（P1 新增）；点击联动切 viewMode='graph' + focusNode。决策：v1 不建独立进度表（防双写不一致），学时统计暂缓。

**P0 已完成（纯前端）**：
- ForceGraph.vue：nodeFill 四档（0 灰/1-29 红/30-69 黄/70+ 绿，masteryLevel() 分档，复用 --color-red/yellow/green）；左下角图例条（graph-legend + path-toggle 开关）；路径高亮（getPathOrder/isPathNode/isPathEdge，前置→后置方向一致才算路径边；applyPathHighlight() 统一刷新，初始渲染+开关切换+hover 恢复三处共用 linkSelection/nodeSelection）；薄弱点脉冲（node-pulse 扩散环，transform-box:fill-box + keyframes，nextNodeId 指定）；**graphFingerprint 纳入 mastery/relation**（否则掌握度变化不触发重绘）
- chatStore.js：新增 learningPath/nextToLearn 状态 + fetchLearningPath()/fetchNextToLearn()（fetchGraph 成功后联动刷新，本地拓扑排序开销极小）；HomeView 传入 :learning-path / :next-node-id / :show-path
- 后端接口验证通过：GET /knowledge/learning-path（ordered_nodes+nodes_detail）、GET /knowledge/next-to-learn（node_id/name/mastery/reason），testuser999(id=1) 有 24 节点图谱数据

**注意**：验证时重置了 testuser999 密码为 demo123456（bcrypt 直写 knowledge.db users 表）。后端 8000 / 前端 5173 已在后台运行。前端需 http://localhost:5173 访问（127.0.0.1 不通，Vite 绑定 localhost）。

**下一步 P1**：后端 GET /knowledge/stats（全局+学科聚合）→ DashboardView.vue → ActivityBar 入口。P2 联动深化（点指标跳图谱+聚焦、NodeDetail 手动调掌握度滑块）。P3 quiz 成绩回写 mastery。

## 学习进度仪表盘 P1 完成（2026-08-31 晚）
- 后端：`/knowledge/stats?subject=` 端点（graph_middleware.compute_stats：total_nodes/subject/overall/by_subject/weak_points Top3/next_to_learn，mastery 单一数据源）
- 前端：DashboardView.vue（总览卡+进度条+学科列表+薄弱点 Top3+下一步推荐）、ActivityBar 新增「学习进度」入口（gauge 图标）、HomeView 挂载 + go-graph/go-node 联动（handleDashboardGoGraph→handleSubjectChange、handleDashboardGoNode→focusNode）
- **关键 bug 修复**：学科切片后 ForceGraph 节点为 0。根因：后端 `get_edges_by_subject` 有意保留跨学科边（一端节点属其他学科），前端 renderGraph 原样传给 `d3.forceLink(links).id()`，d3 初始化找不到悬空端点直接抛 `Error: node not found: xxx`（被 try-catch 吞掉）→ SVG 中断。修复：renderGraph 构建 links 时 `filter(l => nodeIdSet.has(l.source) && nodeIdSet.has(l.target))` 丢弃悬空边。这是 D3 力导向图通用防御，任何切片场景都适用。
- 浏览器实测全链路通过（Console 0 errors）：学科卡片联动（AI 学科 2 节点）、薄弱点联动+聚焦、去图谱学习按钮、筛选学科下拉（stats 过滤 7→2）、全量图谱。TODO.md 仪表盘条目已勾选。
