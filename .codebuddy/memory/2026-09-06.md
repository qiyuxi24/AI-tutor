# 2026-09-06

## Agent Loop 重构 — 调研阶段产出
- 核实 `docs/AgentLoop_重构设计讨论.md`（v0.1 讨论中）诊断与代码一致：llm_client.py `call_llm`(583-673)/`call_llm_tools`(726-810) 二次 tool_calls 被丢；chat_service.py 两阶段撕裂(407-495)；图谱全量注入 system prompt；`fetch_webpage` 同步阻塞；`_run_async` 线程池 hack。
- 产出调研学习文档：`docs/AgentLoop_业界调研与学习路线.md`。要点：业界真循环共识骨架（带 max_turns 的 while + 每次带 tools + 结果回填续跑 + trace 长出，无需框架）；逐家拆解 OpenAI Agents SDK / Anthropic BEA+Claude Code / LangGraph / smolagents / PydanticAI / ReAct / 自家 Votek agent_loop(Rust)；六事实↔业界↔Batch1 对照表；Batch1 三点增量建议：①达 max_rounds 用「注入系统提示+空 tools 强制自然收尾」替代固定报错文案（抄 Votek）；②trace 的 context_tokens 取各轮 response.usage 真值累加；③temperature 循环内统一 0.3。
- 用户选择范围：Batch1 核心循环 ★，但先调研出学习文档，审阅后再实现。Block3（SSE 可见轨迹）仍依赖 qwen 流式 tools 真网验证（DASHSCOPE 403 未恢复）。
- 未写代码；决策记录待用户拍板后进设计文档 §12 + 落 TODO.md。

## Agent Loop 重构 — Batch1 核心循环已实施（同日追加）
- 用户拍板"参考成熟设计、去耦合"后直接实现 Batch1。改动：
  - **新增 `backend/app/core/agent_loop.py`**（~230 行纯编排层）：`run_agent_loop(system_prompt, messages, *, kg, max_rounds=5, tool_timeout_secs=60, temperature=0.3)` 真循环（LLM↔工具多轮串联）；达 max_rounds 不再执行新工具 → 注入"停止调用"系统提示 + 空 tools 强制自然收尾（Votek 同款）；单工具 `asyncio.wait_for(to_thread(...))` 超时护栏；`AgentRunResult(text/rounds/total_llm_calls/context_tokens)`；`save_trace` 落 `backend/data/traces/{uid}/{run_id}.jsonl`（无工具不落盘）。
  - **llm_client.py**：`call_llm` 精简为纯文本（去 enable_tools/kg 参数 + tools 分支）；**删除死代码 `call_llm_tools`**（唯一调用点在 chat_service，已改）；`_run_async` 注释更新。
  - **chat_service.py**：两个入口改走 `run_agent_loop` —— `/chat` legacy `process_message`（result.text 为回复）、`/chat/stream` 后台 `process_background_tools`（+ save_trace）。GraphAnalyzer 触发保留原样（中间态留 Batch5）。
  - 6 处 `call_llm(..., enable_tools=False)` 调用点（graph_analyzer×2 / quiz grader+generator / graph_generator×2）同步清理。
  - `call_llm_stream`（阶段1 流式、不带 tools）不动，Batch3 合并可见回答前保留"先答后做"过渡。
- 测试：新增 `tests/test_agent_loop.py` 7 用例（mock `_chat_once`+`execute_kg_tool`），全库 **245 passed 零回归**。坑：OpenAI 响应 usage 在 **response 层**非 message 层。
- 设计文档 `AgentLoop_重构设计讨论.md` 已更新状态 + §9 Batch1 标 ✅ + §12 决策记录 #1-#5。TODO.md 主清单未动（Batch2 图谱按需注入 L1 / messages 裁剪、Batch4 eval_agent_loop.py 待排期）。

## Agent Loop 就绪审查 + 架构落档（同日）
- 就绪审查发现并修 2 个真 API 联调前缺口（决策 #6）：① 自然终止空 content 无兜底 → 补引导文案；② force_finish 原用 system 消息追加，改 **user 消息**（规避兼容网关多 system 不确定）+ 该次调用包 try（失败退化固定文案）。测试 9 用例，全库 **247 passed**。
- 架构已"撇清"落档：设计文档新增 **§13 当前架构全景**（调用链 ASCII 图 + 模块边界表 + loop 就绪对照 + 真实 API 联调 6 项检查点）。关键认知：**run_agent_loop 目前只驱动后台工具通道，尚未成主答案生成器**——/chat/stream 仍两段（阶段1 流式 + 阶段2 后台 loop），无工具时阶段2 是探测性调用；合并 = Batch3（需 qwen 流式 tool_calls 验证）。legacy /chat 未接 save_trace（非推荐路径，有意留空）。
- 下一步：用户待调真实 API 测试（检查点 1-6）；若 DASHSCOPE 403 已恢复则逐项验。

## LLM 环境实况（真实 API 冒烟发现）
- **对话主模型已整体切到 MiniMax**：根 `.env`（唯一真值，config `parents[3]/.env` 读取）配 LLM_BASE_URL=https://api.minimaxi.com/v1、MODEL_NAME=MiniMax-M3；`config.py` 有 `llm_api_key`(LLM_API_KEY) 字段，`llm_client.client` 取 `llm_api_key or dashscope_api_key`（空回退）。嵌入 embed_client 固定阿里 DashScope text-embedding-v4（与对话解耦）。config.py/llm_client.py 顶部为此在会话期间被更新过（新版本）。
- 新增 `backend/scripts/smoke_agent_loop.py`（agent_loop 真 API 冒烟：--user/--prompt/--expect-tool）。
- 首次冒烟 = MiniMax **401**：根因 = `.env` 的 LLM_API_KEY 空 → 回退发 DASHSCOPE_API_KEY 的旧 key → MiniMax 不认。等用户填有效 MiniMax key 到 LLM_API_KEY 后重跑。loop 错误链本身验证 OK（401 → E-LLM-003 正常映射，未崩溃）。
- 旧记忆"DASHSCOPE qwen-plus 403 free quota"仅对阿里对话模型过时；嵌入仍依赖阿里 DASHSCOPE_API_KEY（需非欠费）。

## agent_loop 真实 API 冒烟通过（MiniMax-M3，同日）
- 用户填入有效 LLM_API_KEY 后重跑 `scripts/smoke_agent_loop.py --user 9999` 全通：
  - round0 模型主动 rag_search("递归",graph)（空库友好返回）→ round1 add_knowledge_node(hanoi_tower) **真实落库成功** → round2 自然收尾；total_llm_calls=3、context_tokens=7453(usage 真值)、trace 落 data/traces/9999/*.jsonl。
  - **教科书级 agent 行为**：先检索发现无前置知识 → 再建节点 → 建议补"递归"节点建 prerequisite。
- 冒烟暴露并解决的坑：`nodes.user_id REFERENCES users(id)` 外键 → 直接用未注册 user_id 建节点报 FOREIGN KEY constraint failed；smoke 脚本已自备 users 行（INSERT OR IGNORE），正常路径由注册流程建。
- 首次冒烟顺带验证：max_rounds 达上限后 **force_finish 真实触发**（MiniMax 以 `<think>` 思考 + 自然收尾）；MiniMax-M3 支持标准 function calling 协议（assistant(tool_calls)+tool 多轮回填无 400）。RAG graph 检索 8s 单源超时降级正常。
- 真实 API 联调检查点（设计文档 §13.4）状态：① 多轮工具协议 ✅ ② force_finish ✅ ③ 空 content 兜底(未触发,代码路径有) ④ trace 落盘 ✅ ⑤ 全链路(前端 SSE 未测) ⑥ usage 真值 ✅。

## MiniMax 接入调研 + chat/embed provider 解耦（同日追加）
- 用户拿到 MiniMax 国内站 API（qwen 聊天额度 403 想换 MiniMax 做对话/Agent 主模型，embedding 保留阿里 text-embedding-v4）。关键认知：**MiniMax 已全面 OpenAI 兼容**（国内 base `https://api.minimaxi.com/v1`，国际 `api.minimax.io/v1`，Bearer key 无需 group_id）；官方域名易混淆（platform.minimaxi.com=国内 / platform.minimax.io=国际，网传 api.minimax.chat 多为第三方代理）。五大方向接口：语言(chat，M3 1M ctx/M2.7/M2.5)、语音(T2A Speech-2.8)、图像(image-01)、视频(H3)、音乐(2026-08-20 起付费接口不对新用户开放)。与 OpenAI/阿里差异：n 仅 1、不支持 presence/frequency_penalty、M 系思考内容默认裹 `<think>`、reasoning_split=True 拆 reasoning_details、tool 多轮须保留完整 assistant 对象。
- **结构坑→根因修**：全后端单一 OpenAI client `llm_client.client` 同时服务 chat(MODEL_NAME) 与 embedding(text-embedding-v4，阿里独占)；直接把 LLM_BASE_URL 切 MiniMax 会连带打挂嵌入。故拆分：
  - config.py：新增 `llm_api_key`(LLM_API_KEY，对话 key，空则回退 DASHSCOPE_API_KEY) + `embed_base_url`(EMBED_BASE_URL，默认阿里 compatible-mode)；dashscope_api_key 语义收紧为仅嵌入。
  - llm_client.py：`client`(chat) key 用 llm_api_key；新增 `embed_client`(AsyncOpenAI，dashscope key + embed_base_url)。
  - rag/manager.py、kb/kb_manager.py：`import embed_client as client`（调用行零改动）；embedder.ApiEmbedder base_url → settings.embed_base_url。
- 新脚本 `backend/scripts/test_minimax.py`：自检 chat/stream/tools 三场景，读 backend/.env 的 LLM_API_KEY/LLM_BASE_URL/MODEL_NAME（不混用阿里 key），--key/--base/--model 可覆盖。已验证 py_compile、--help、import 链路、test_rag_tool 8 passed。
- 用户待办：backend/.env 取消注释填 LLM_API_KEY/MODEL_NAME=MiniMax-M2.5/LLM_BASE_URL=https://api.minimaxi.com/v1 → 跑 test_minimax.py 确认模型名与 base 正确 → 重启后端即生效。是否做自动降级切换（qwen↔MiniMax 双 provider）暂未做（YAGNI）。

## env 统一 + MiniMax 配置就绪（同日 2）
- 用户确认：①env 统一为**根 .env 唯一真值**（本地+Docker 共源）；②MiniMax 多模态 RAG 暂不接入，只把配置/env 写好待其填 key 测试。
- 落地：根 `.env` 写真值（迁移原 backend/.env 的 DASHSCOPE key + SECRET；对话切 MiniMax-M3，LLM_API_KEY 留空待填并加醒目注释）；根 `.env.example` 重写为唯一全量模板；**删除 backend/.env 与 backend/.env.example**；`config.py` `_ENV_PATH` 改 `parents[3]/.env`；`test_minimax.py` 改读根 .env（parents[2]）；`docker-compose.yml` environment 增 LLM_API_KEY/EMBED_BASE_URL 且默认改 MiniMax（DASHSCOPE 仍强制，嵌入必需）；install.ps1/README.md/docs/简历强化 引用全部从 backend/.env 改根 .env。全库搜残留零匹配，lints 干净，py_compile 过。
- MiniMax 生态确认（2026-09）：M3=最新（1M ctx/图、视频 image_url content 块）；M2.7/M2.5 备选；有自有 embedding（embo-01）未启用。模型名大小写/国内 ID 待用户真 key 联调校验（测试脚本 --model 可覆盖）。
- 用户待办（更新后）：在**根目录 .env** 的 `LLM_API_KEY=` 填 MiniMax key → `backend\venv\Scripts\python.exe backend\scripts\test_minimax.py` → 重启后端。切回 qwen：LLM_BASE_URL 改回百炼 + LLM_API_KEY 留空。

## env 统一耦合检查 + MiniMax 真实联调通过（同日 3）
- **env 统一后全库耦合扫描**：app 层 env 仅 config.py 一处读取（无散点 os.getenv）；tests 无 env/load_dotenv 依赖；Dockerfile COPY backend → 容器内 parents[3]=/app，无 .env 文件 → load_dotenv 找不到文件不覆盖注入 env（行为与旧 backend/.env 时代一致）；entrypoint.sh 纯注入不读文件；compose 已显式注入 LLM_API_KEY/EMBED_BASE_URL；.dockerignore **/.env 排除无泄漏；start/install 脚本路径已同步。唯一残留修复：test_minimax.py 引导文案 "backend/.env"→根 .env。全库零残留引用。
- **MiniMax-M3 真实联调全绿**（用户已在根 .env 填 LLM_API_KEY）：chat/流式/工具调用(get_weather)三场景通过，usage 正常。⚠️ **关键发现：M3 默认把推理内容以 `<think>...</think>` 文本包裹进 message.content**（流式 chunk 也是 content 增量）→ 前端会看到思考过程、会话持久化/Agent loop 上下文也会混入。官方支持 reasoning_split=True（extra_body）把思考拆到 reasoning_content/reasoning_details，或后置剥离 `<think>` 标签。**用户已定明日（9/7）处理**（影响对话体验/上下文膨胀，建议 Batch3 合并时一并处理）。
- **`<think>` 适配已记 TODO.md**（P1 首条，注明 9/7）：方案 A 剥离标签 / B reasoning_split=True；触及点 call_llm_stream / agent_loop 回填 / chat_service 存储 / 前端。同步更新 TODO「致命阻塞」状态行：qwen 403 已由 MiniMax-M3 化解，残余依赖仅阿里 embedding 额度。
