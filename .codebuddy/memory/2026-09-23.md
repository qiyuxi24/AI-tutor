# 2026-09-23（蒸馏版，2026-09-24 压缩；细节见 `docs/知识图谱/` 各文档）

## 同学分支 `2d51aee "z2"` 评审与合并
- 内容（1 提交混 5 主题，37 文件 +3244/−81）：① `core/open_source.py`（来源能否留存的**唯一判定出口**，fail-closed 到 L3）+ 3 个接线点（采集默认 L2→**L3**、web_archive 预筛、download 前判定）；② `agent_tools/web_archive.py`（联网正文 → 图谱节点 `origin="web"`，URL 派生 ID 幂等）；③ 维基正文修复（模板取末参数、清魔术字、外链转 MD、**zhconv** 简繁统一）；④ KB 正文预览 `GET /kb/node/{id}/text` + `KbTextPreviewDialog.vue`（DOMPurify 消毒）；⑤ `start.ps1` 加固 + ASCII-only `start.cmd`。
- 质量问题：提交信息无信息量；3 个 `_tmp_*` 临时文件入库；**测试硬依赖可选依赖 zhconv**（代码是"未装则跳过"，测试却没 `importorskip`）。
- 合并落地：worktree 隔离验证 → `44d3d84` merge（唯一冲突 `tests/test_node_write_paths.py`，解法 = 保留 main 的 GQ-1 块 + 采用同学侧"五条路径"标题）→ `673e008` 清理提交（删临时文件 + 补 `importorskip`）。**912 passed，未 push**。
- 评测复核：`eval_rag.py --embed mock` 结果与 09-12 基线**逐位一致**（vector R@1 0.470 / bm25 0.964 / hybrid 0.766 / fuse 0.818），合并未退化检索。缺口：本次改动没接任何评测集；许可判定误杀率无量化；已入库的 11 个采集文件仍是旧文本（需重采才见效）。

## 图谱数据结构评审（纯文档）
- 新建 `docs/知识图谱/知识图谱_数据结构与检索通路_评审与改良方案.md`（15 项问题分级 + KG-D1~D15）。P0 六项：学科靠 `tags` 猜 → 升 `nodes.subject` 列；难度档占 tags；无 part_of 层级；无别名层 → `node_aliases`；mastery 无证据链 → `mastery_events`；图谱检索单腿（嵌入欠费即返空）→ 关键词兜底。
- 实测（本机 SQLite 3.50.4 内置 FTS5）：`tokenize=trigram` 中文 **≥3 字命中**、**1–2 字不命中** → 定 **LIKE 兜底先行**。
- 明确不改：`nodes.id` 全局 TEXT 主键（改复合键要动 21 个端点 + 前端，收益低）。

## KG-D 施工（本日两批）
- **D1/D2/D3/D5**（用两个 kiwi 子 agent 并行 + 我验收）：`nodes.subject` 列 + 回填 + `idx_nodes_user_subject`；tags 只放学科（清掉"诱导写难度档"的文案）；`node_aliases` + `find_node_by_name` 先查别名；`rag_manager.search` 嵌入失败 → `_keyword_search`（**查节点本体 name/summary，不能查 chunks** —— 嵌入失败时节点根本没进 chunks）+ `chunk_index` 改节点内全局递增。**927 passed**。
  - 子 agent 漏的两处闭环（我补）：`create_node_from_ai` 落 subject、`kg_taxonomy.assign_taxonomy_sync` 把判定出的学科写回 subject 列（否则 tags 首个自由标签会被 `add_node` 兜底误判成学科）。
- **D4/D6/updated_at**（**948 passed**）：`edges` 补时间戳 + `UNIQUE(user_id,from,to,relation)`（建索引失败**只 warning 不阻断启动**，否则存量重复边会让后端起不来；`IntegrityError` 映射回 `ValueError` → API 409）；`mastery_events`（**唯一写入点收敛到 `update_node_info`**，5 条改 mastery 的路径全经它，新增路径不用自己记账；只记"真的变了"）；`nodes.updated_at` 补列 + 三处刷新。
  - **刻意未做**：`weight`（当前无任何写入方 → 会变成第二个 `confidence`）、`relation` 触发器（两条写路径已守）。
- 真实库验收：重复边组 = 0 → 唯一索引建成；`updated_at` 回填 348/348；`inspect_graph_quality.py --user 5` 与基线一致。

## 嵌入咨询（未改代码）
- 嵌入 key 只有根 `.env` 的 `DASHSCOPE_API_KEY`，被检索侧与去重侧共用；**模型名无 env 开关**（`EMBEDDING_MODEL` 等硬编码在 `llm/embed.py:30-33`，`ApiEmbedder` 反向引用 → 改一处改两侧）。
- 百炼 API **不存在 `qwen3-embedding-8b`**（那是开源权重，要自部署后改 `EMBED_BASE_URL`）；托管版就是 `text-embedding-v4`（1024 维，batch 10）。
- ⚠️ **换任何嵌入模型必须重建索引，同维度也不行**（现有代码只在维度不符时 skip → 同维多模型会静默出垃圾结果）。
- ✅ 用户充值后**嵌入已恢复**（200 OK，1024 维，cos(栈,队列)=0.7756）；降级期入库的 NULL 向量块**不会自动补**。

## 🚨 图谱开/关 A/B 重跑（本日最重要）
- 发现文档漂移：`eval_graph_injection.py` 与首跑结果（25.0%→62.5%）**早在 09-12 就有**，但《设计说明》与 MEMORY 都写"未做" → 已修正。
- 数据集 5 → **14 例**；脚本加 `--report-only` / `--rescore` / **断点续跑**（14 例 × 2 组一次跑不完 → 分 `--groups off/on` 两次合成）。
- **首跑的提升未复现，根因是指标缺陷**：注入图谱后模型倾向抄 `node_id` 指代节点（语义正确但没中文名 → 判 0），而关闭组看不到图谱只能猜名字、猜中反而计分 → **指标同时惩罚正确行为、奖励猜测**。
- 修正口径：定位率（中文名 ∪ node_id，带词边界）+ 引用真实 node_id（机制证据）+ 状态误述（`state_misclaim`，拿 `nodes.mastery` 真值对照）。
- 重跑（14 例 × M3）：引用真实 node_id off 0/14 → on 5/14（机制证据）；**定位率 39.0% → 36.6%（Δ −2.4pp，持平）**；检索 42 → 0 次；**token −65%**；状态误述 2 → 0 例。
- 诚实结论：两次运行结论相反 → **该指标不稳定**，对外引用前必须多次重复取均值 + 锁定模型版本（记为 GQ-14）；"定位率"只答"有没有提到"，不判对错与讲解质量。
- 用户拍板：D7 层级 = **真实章节点 + 物化 path 两者都做**（排在 A/B 之后）；无向归一化**推迟**。离线全量 **962 passed**。
- ⚠️ 工作区同时有**另一 AI 会话的改动**（whoosh/kb_manager/vector_store/eval_* 等）→ 提交必须路径限定。
