# 2026-09-22

## 分支合并与存档（main ← zhuaqu）

- **存档**：本地 WIP 分 5 个提交（docs 目录归位/校准、backend 日志-记录层收敛、测试与脚本、`docs/比赛` 现状、done.md+运维后台用户页+探针），rebase 到 `origin/zhuaqu` 后 push。
- **冲突 4 处**（`README.md`、`README.en.md`、`TODO_Collector.md`、`TODO_Context.md`）手工合并，策略 = 取并集：保留远端 Wiki 条目 + 本地新路径 `docs/工程实践/README_编写规范.md`；TODO 用新目录路径 + 较新日期（09-20）；`TODO_Context.md` 保留"八模块"表述。无残留冲突标记。
- **结果**：`origin/main` 已含全部改动；`origin/zhuaqu` 完全并入 main（0 ahead / 5 behind）；**远端 zhuaqu 分支未删除**（用户未要求）。
- **环境补齐**：本地 venv 装 `trafilatura` 2.2.0 + `readability-lxml`（同学新增依赖，`backend/requirements.txt` 已列，未装则 `test_web_page_adapter.py` collection error）。
- **验证**：`pytest backend/tests -q -m "not llm_api"` → **835 passed, 7 deselected**（原口径 745，两侧测试相加）。据此校准 AGENTS.md / README / README.en / CONTRIBUTING(.en) / TODO_Assessment / TODO_Context / README_编写规范 的基线数字，提交 `772a524` 并 push。
- 备注：`docs/比赛/` 的部分文档删除随 `be7a6cb` 一并存档（用户未点名处理比赛材料，如需保留可 revert 该提交）。

## 依赖核查：为什么有 opencv（结论：传递依赖，非直接使用）

- 代码库（排除 venv/node_modules）搜 `opencv|cv2` → **0 命中**，项目从不 `import cv2`。
- 来源：`pip show opencv-python` → `Required-by: rapidocr-onnxruntime`（RapidOCR 内部用 cv2 做图像预处理）。链：`rapidocr-onnxruntime 1.2.3` → `opencv-python 5.0.0.93`。
- 语义：`backend/requirements.txt:26-27` 的 `rapidocr-onnxruntime` / `onnxruntime` **未注释** → `pip install -r` 必装；注释写"可选/装则启用"与实际"安装层必装、仅运行层降级"不一致。
- 用途：知识库上传图片 / 扫描版 PDF 的 OCR（`kb/parsers/ocr.py::have_ocr()` 探测，失败返空；`parsers/__init__.py` 仅 `installed()` 才注册 `ImageOcrParser`）。
- 体积实测（site-packages）：cv2 112.4MB + onnxruntime 43.1MB + rapidocr 13.2MB + shapely 6.0MB + pyclipper 0.2MB ≈ **175MB**；`docs/运维部署/运维_生产上线与日常运营指南.md:694-700` 已把"注释掉 OCR 依赖"列为镜像瘦身首选 → 属有意带上。
- 未擅自改 `requirements.txt`（影响部署，待用户选：A 保留 / B 注释两行换取 ~175MB / C 换 headless 版不推荐）。
