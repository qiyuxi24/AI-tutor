# 2026-06-19

- 完成全代码审查，生成详细审查报告
- 创建 `TODO.md` 改进清单，按 P0/P1/P2/P3 优先级排列共 16 项
- 初始化 MEMORY.md 记录项目关键信息
- **P0 完成**：加固 `.gitignore`（密钥/日志/DB/缓存等），默认管理员密码改为从 `DEFAULT_ADMIN_PASSWORD` 环境变量读取
- **图谱搜索 + 新手引导完成**：
  - 新增 `GraphSearch.vue`：即时搜索、键盘导航、高亮匹配、搜索后聚焦节点到图谱中心
  - 新增 `OnboardingGuide.vue`：3 张卡片（欢迎/图谱/对话）、步骤指示器、跳过/上一步/下一步、localStorage 记住状态、Teleport 到 body
  - ForceGraph 新增 `focusNode()` 暴露方法（平滑缩放 + 脉冲高亮）
  - HomeView 集成两个组件，去耦合设计
- **权限保护修复**：`PUT /knowledge/node/{node_id}` 的内容更新从直接 `open().write()` 改为统一走 `kg.update_node_content(node_id, content, mode=op, caller="human")`，不再绕过 KG 的 `_guard_human_content` 权限检查
- **模式精简**：三种模式合并为两种 —— `adaptive`（自适应引导，灵活使用拆解/追问/角色反转/类比）+ `free_talk`（自由对话，可直接解释，一次讲透一个逻辑闭环）
  - 新增 `system_prompt_adaptive.j2`、`system_prompt_free_talk.j2`，删除旧三模板
  - 更新：`GuideMode` 类型、`MODE_TEMPLATE_MAP`、前端 MODE_OPTIONS/MODE_LABELS、默认值
- **知识点跳转链接完成**：
  - MessageBubble：AI 回复渲染后自动扫描匹配知识节点名称，注入 `.kg-link` 可点击标签（虚线边框、hover 高亮），点击 emit `navigate-to-node`
  - NodeDetail：前置知识/相关节点 tag 改为 `<button>`，点击 emit `navigate-to-node`，关闭当前弹窗 → 加载目标节点详情 → 在图谱中聚焦
  - ChatArea 透传 `knowledgeNodes` + 事件，HomeView 统一编排跳转逻辑
