# 2026-09-15（蒸馏版，2026-09-24 压缩；原文细节已下沉到各 docs）

## 上下文工程
- 递归模式重复图谱注入块**删块**（非加上限）：`system_prompt_recursive.j2` 删「当前知识图谱框架」整段。实测 300 节点 28,581→11,952 字符（−58%）。代价：recursive 不再预注入节点标签与"只列前置边"视图。
- 🔴 **占位符事故**：`system_prompt_common.j2` 的图谱占位符在 09-14 前是单花括号 `{knowledge_graph_summary}`（Jinja2 只认 `{{ }}`）→ adaptive/free_talk 的图谱**从未真正注入**，无报错，只表现为"AI 说图谱是空的"。守卫 `test_prompt_loader.py`（断言值被替换 + 字面占位符不残留）。
- 预算框架 v1→v2：用户定案 **B=48K**（不再套"有效长度 50%"折扣），段数 8→9（S6 图谱检索 / S7 知识库检索拆开记账）。让位顺序 历史→检索→图谱→画像。SSOT = `docs/上下文工程_预算框架.md`，活清单 = `TODO_Context.md`。
- 实测基线（`reports/probe_context_budget.py`）：固定成本 S2–S7 = **13,984 token = 29.1%B**；工具 schema ≈222 token/个 ×12，每轮必发。
- 文献依据：RULER arXiv 2410.18745（多数模型有效长度 < 标称 50%）、Databricks 2411.03538（检索正确率峰值 8–32K、倒 U）、Lost in the Middle 2307.03172（中间位 52.9% 低于闭卷；query 双端放置 45.6%→100%）。
- 两个查证事实：① 根 `.env` 没设 `LLM_CTX_BUDGET` → 改 `config.py` 两处（dataclass 默认 + `from_env`）；② **P0-③ 落点坑**：`guard` 只能裁 messages、修不了 system prompt 超预算 → 必须落在组装侧重建图谱摘要。

## 上下文管理落地（P0 全 + P1 全，七项完成）
- P0：`LLM_CTX_BUDGET` 32000→48000、`OUTPUT_RESERVE` 2000→3000；固定段 >40%×B 告警、>45%×B 强制减半重建图谱注入。
- P1：S6/S7 按源独立截断（3000）；S8 分层保留（省略说明 + 首条 user 锚点 + 最近 3 轮）；Tool Result Clearing；query 双端放置（≤300 字符）。
- 🔴 **TODO 里 P1-③ 的落点判断是错的**：跨请求历史里永远没有 tool 消息（`ChatMessage` 只有 role/content，`build_api_messages` 会剥掉 `tool_call_id`）→ 清理放 guard = 死代码。正确落点 `agent/context.py::clear_old_tool_results`（按"工具批次"分组，保留最近 2 批）。
- 真机：分层历史 17,073 → **1,186 token（−93%）**，API 接受、作答正常。

## 重构与收口
- `core/` 平铺 5 文件 → `core/agent/` 包（loop / context / guard / events / store / estimator + README），用 `git mv` 保历史。
- 工具系统：`core/agent_tools/` —— `registry.py` 零 import、`dispatch.py` 只认 spec 契约、`tools/` **一工具一文件**（五段：DESCRIPTION/PARAMETERS/GUIDANCE/handler/SPEC）、`net_guard.py` 为 SSRF 能力层。等价性证据：`KG_TOOLS` 仍 12 个、`TOOLS_PROMPT` 仍 3148 字符；手写块 3991 → `TOOL_POLICY_PROMPT` 1180。
- 工具实现归位 `impl/` → `tools/`，对外 API 零变化。新增 4 份模块文档（agent / agent_tools / tools / llm / mcp_servers README）。
- `token_estimator` → `agent/estimator.py`，接线落 `agent_runs.token_estimate` 列（用 `PRAGMA table_info` 补列，老库实测成功）；预估**不参与预算决策**。

## 图谱写路径收口
- 唯一出口 `KnowledgeGraph.create_node_with_content`（先 add_node 再写 MD，ID 冲突不留孤儿文件）；MD 模板唯一来源 `ORIGIN_NOTES`；4 条写路径全改走它。新不变量：节点 MD 写入只剩 `knowledge_graph.py` 两处。守卫 `tests/test_node_write_paths.py`（8 例）。
- 同名并轨 `_find_same_name` 精确化（真实库曾出现 `harmony_dev_intro` / `harmonyos_intro` 同名并存）。
- 真机（user 5）建出 `kmp_algorithm`、`minimum_spanning_tree`。
- ⚠️ 未修：`agent_runs` 落在 `backend/app/data/agent_runs/`，而 compose 卷只挂 `./backend/data` → 容器重建丢审计记录。

## 死文件审计 / 嵌入双源
- 删 `core/kb/parser.py`（零引用）。未接线：collector/pipeline_ingest + chapterizer、collector/quiz_splitter（登记 TODO，未擅自删）。
- 嵌入双源收口：去重侧 `ApiEmbedder` 改用检索侧同名常量 + `MAX_EMBED_CHARS` 截断 + 失败语义改"条数不齐→整批 []"；**不做同步桥**（FastAPI 同步上下文里跑新事件循环 → "Event loop is closed"）。文档措辞统一为"**检索侧**唯一出口"。

## ponytail A/B（详见 `docs/工程实践/AI编码Skill_ponytail实测与通用Skill选型.md`）
- 同任务 A/B：A 无 skill diff +27/−22（1,062 token）；B 有 skill diff +7/−4（257 token）+ 技能加载 2,005 token → **本任务 B 净亏 ~1.1K token**。回本阈值 ≈ 不省则要多写 >2K token 的代码。
- 用户拍板最终用 **A 版观感**（final diff +26/−21，964 token）。
- 外部证据：ponytail.dev 自报数据无复现路径；arXiv 2503.20197（>90% 代码缺陷来自"缺失检查"，无"过度设计"类）→ **不得宣称"更稳定"**；ETH 2602.11988（AGENTS.md 不提成功率、成本 +>20%，**概览型内容无用**）→ 据此给 AGENTS.md 瘦身 334→101 行（8,840→~4.3K token）。
- 补装 superpowers skill：test-driven-development / verification-before-completion / systematic-debugging / brainstorming / writing-plans / executing-plans。未装 spec-kit、BMAD。

## 协作与提交
- 合并 `pr/1` → main（merge `6fe427c`），2 处冲突按"保语义"融合；推 origin 成功，**推同学仓库 permission denied（只读）**。
- 首次全量提交 `5d96f76`（90 files, +5108/−1490），提交前 711 passed。
- ⚠️ push 三通道实测：直连 443 阻断；代理 `127.0.0.1:7897` TLS 握手失败（时通时断）；SSH 可达但无密钥。待代理恢复后 `git -c http.proxy=http://127.0.0.1:7897 push origin main`。
- 并发教训：**改共享大文件前先重读**（AGENTS.md 被另一会话同步瘦身，预备补丁作废）。
- 测试数字演变：671 → 678 → 680 → 686 → 699 → **711 passed, 7 deselected**。
