# 2026-09-08

## Agent 工作可视化模块 · 第一步（后端事件↔记录整合）落地
- 前置讨论：梳理出旧体系三大病根（3 份互不关联记录 / 3 个执行入口仅 1 条可观测 / EventBus 纯内存易失），用户拍板：新建表 + 证据级 + 先解决文件冗余。
- 实施（详见 MEMORY.md「Agent 运行记录整合」）：新建 `agent_run_store.py`（agent_runs 表，SQLite WAL）；`run_agent_loop` 传 user_id 自动落库（ok/error 都落，db_dir 可注入）；证据级 evidence（thinking 全文/完整 args/完整 tool 返回）；事件带 run_id；删 jsonl save_trace 与 process_background_tools 死代码；新增 GET /api/v1/agent/runs + /{run_id}；token_estimator 改读表。
- 验证：backend 全量非 llm_api 测试 461 passed（新增 test_agent_run_store 5 例；唯一挂 = test_token_counter none_content，既有 tiktoken 退化估算偏差 8vs7，与本次无关）。
- 用户后续关注：前端 agent 工作可视化组件（参考 Votek 的 Codex/Claude Code 步骤流风格）；对话内嵌 tools/thinking 字段去留、trace 清理/GC 待第二步讨论。
- 收尾清理：删除废弃 `data/traces/` 目录（残留 2 个旧 jsonl，smoke user 9999；代码已零引用、展示/预估全走 agent_runs 表，.gitignore 本就忽略）。文件冗余清零。
- **trace 清理策略已落地（后端侧三步，前端留第二步）**：① agent_run_store 库目录迁 `backend/data/agent_runs`（parents[2]，随 Docker backend/data 卷持久化）；新增 `prune(full_days=30, strip_days=180)` 分层保留（近期完整→中期摘 evidence→远期删行）、`delete_run`/`delete_user_runs`/`count_runs`/`run_stats`、list_runs 加 offset 分页。② main.py 新增 on_event startup（启动先 prune + 24h 后台每日任务，shutdown cancel）。③ agent_runs.py 路由重排（stats 放 {run_id} 前）+ 分页 total + stats + DELETE 单条/清空本人。测试 464 过（原 5→10 例，含 prune 幂等/跨用户、越权删除）；TestClient 启动冒烟通过，backend/data/agent_runs/agent_runs.db 已真实生成。待办下一步：前端 tools/thinking 摘除（随可视化一起）、run↔conversation 关联列。
- **以 loop 为核心的 API/架构审查完成 + 根目录 AGENTS.md 开发文档落地**（2026-09-08）。审查结论：loop 编排本身干净（agent_loop 367 行），复杂集中在 ① llm_client.py 上帝模块 727 行（客户端+工具定义/执行+RAG/抓网页+重试+两代 chat）；② core→api 反向依赖（execute_kg_tool→knowledge.create_node_from_ai、chat_service→knowledge._apply_suggestion 等私有符号）；③ 工具能力文案 3 处分散（KG_TOOLS+execute_kg_tool 分支在 llm_client、TOOL_CAPABILITY_PROMPT 在 chat_service）——加一个工具要改 3-4 处；④ 事件协议无 schema，chat_service 白名单 if/elif 无 else（新事件静默丢弃）；⑤ token 指标 context_tokens/token_usage 双存冗余（向后兼容保留）；⑥ agent_runs REST 5 端点目前零前端消费（等可视化）。新建根目录 `AGENTS.md`：面向后续写代码/改架构的开发索引（运行命令、10 个 router 端点总表、run_agent_loop/AgentRunResult 契约、SSE 事件协议表、agent_runs 表、工具新增 checklist、已知耦合 9 条清单、设计文档索引）；REST 细节指引到 FastAPI /docs 不重复。用户拍板以该文件作为以后开发查阅+改架构依据。
- **core→api 反向依赖消除（AGENTS.md §3.6-1 完成，2026-09-08）**：`create_node_from_ai`/`_apply_suggestion`/`_load_suggestions`/`_save_suggestions` 四个纯业务函数从 `app/api/v1/knowledge.py` 下沉到新模块 `app/core/knowledge_writer.py`（AI 写图谱层，公开命名：create_node_from_ai/apply_suggestion/load_suggestions/save_suggestions，含 __all__）。更新调用方：agent_tools.py（顶部 import create_node_from_ai，_h_add_node 去掉函数内 lazy import，头部注释补一行重型实现映射）、chat_service.py（改 import core 公开名 + 3 处调用点）。api 层删除两段死代码（建议持久化块 + create_node_from_ai/_apply_suggestion 200 行）与 json/Path 无用 import。AGENTS.md §3.6-1 已划线标记完成。验证：py_compile 过、lint 零错、backend 全量非 llm_api 测试 482 passed（较上次 464 多出为新合并用例）。
- **工具系统收敛为注册表驱动（2026-09-08，解决审查③）**。判断澄清：MiniMax/OpenAI 兼容模型 API 原生消费 `tools`（function calling），不消费 MCP；引入 MCP server 运行时是绕路（单机单体、无外部 agent 消费方）。真正不规范=定义/分发/提示三处分散。落地：`llm_client.py` 新建 `_TOOL_SPECS` 注册表（每条 name/description/parameters JSON Schema/handler，结构同构 MCP tools/list inputSchema，ponytail 注释标注未来可导出 MCP server）；8 个工具拆为 `_h_*` handler（add_node 仍内联 import api 层 create_node_from_ai，更新 profile 内联 UserProfile，fetch/rag 调本模块函数）；`KG_TOOLS` 由注册表生成（对外形状不变，agent_loop/测试零改动）；`execute_kg_tool` 改为查 `_TOOL_BY_NAME` 数据驱动分发 + 保留 ValueError/PermissionError/Exception 三级兜底与错误码。`TOOL_CAPABILITY_PROMPT`（chat_service）保留不自动生成——它是"何时主动调用+权限"教学语义层，自动生成会丢语义（有意分离，AGENTS.md 已注明）。验证：test_rag_tool/test_agent_loop/test_minimax_thinking/test_event_bus 30 passed；模块加载冒烟 8 工具与索引一致；分发兜底 3 路径（未知工具/参数解析失败/异常兜底）冒烟符合预期。AGENTS.md §3.6-5 与 §4 已更新（新增工具=注册表 1 处 + 可选 prompt 语义条目）。
- **消息发射中间件 `app/core/agent_events.py` 落地（2026-09-08，三分法第一步）**。用户提出 agent_loop 的上下文管理/上下文存储/消息推送应各自独立、推送是公开中间件；先做推送层。落地：新建 `AgentEventEmitter(run_id, user_id)`（emit 自动注入 run_id、per-user 路由、user_id 为空静默不发布——顺带修正原实现"user_id=None 时 publish 无 user 参数走全局广播"的语义泄漏，对齐 run_agent_loop 文档"不推事件"）；`run_agent_loop` 新增可选 `emitter` 参数（默认按 user_id 构造，注入即接管分发，鸭子接口 emit(type, **data) 即可，未定义 Protocol/接口）。agent_loop 内 8 处 `publish_event(...)` 直调全部改为 `emitter.emit(...)`；`_loop_core/_force_finish/_execute_tool` 签名由分散的 `user_id+run_id` 收敛为单一 `emitter` 参数；agent_loop 不再 import event_bus（事件类型常量改经 agent_events re-export，常量唯一来源仍在 event_bus）。验证：test_agent_loop.py 12 passed（原 9 + 新增 3：emit run_id 注入+user 路由 / 无 user 静默 / 注入 emitter 收齐事件序列 start→tool_start→tool_result→text_delta→done）；lint 0；模块导入冒烟 OK。调用方 chat_service 零改动（默认参数兼容）。后续：上下文管理独立文件（Batch2 裁剪）与 agent_events 分发扩展待做。

## Loop 架构体检 + OpenClaw 对比 + 机械脚本清理判定（2026-09-08 晚）
- 体检结论：编排/事件/落库/工具四层已分离，run_id 贯穿事件与 agent_runs，护栏齐备，架构干净。已知债务（AGENTS.md §3.6）：agent_loop import llm_client 5 个下划线私有、core→api 反向依赖、token 双存冗余。
- 对标 OpenClaw（MIT 本地优先 Agent 网关，Gateway→Lane 队列→执行循环三层分离）给出差距清单，按收益排序：①**上下文窗口守卫与压缩**（我们 messages 无限拼无窗口预检——最值得做，长对话 token 成本可量化）；②**模型回退链**（现只同模型重试；可借鉴 Votek model-quota-exhausted）；③HITL 审批门控（中期可选，接"待审核建议"UI）；④结构化输出 json schema（低优先，先验 MiniMax json mode）；⑤并行工具/会话调度队列=教学场景**不做**（YAGNI）。我们的强项=agent_runs 证据级记录+事件可观测（对标其审计+control UI），前端可视化即已排的第二步。
- "机械脚本"清理判定：旧"两段式固定脚本"残余只剩 ①`llm_client.call_llm_stream`(670-725，55 行) **确认死代码**（零调用，chat_service 仅 import 未用，测试零引用）；②过时注释 3 处（chat_service 模块 docstring、llm_client.py:6、667-668"阶段1"）；③`/chat`+`process_message` **建议保留**（公开契约/curl 演示用）。GraphAnalyzer/decompose 是独立旁路非机械脚本，保留。清理方案=删 call_llm_stream + 无用 import + 修注释（净删约 60 行零行为变化），已向用户确认是否执行。
- **死代码债务清理已执行（2026-09-08，用户确认）**：①删 `llm_client.call_llm_stream` 死函数（原 666-724，无任何调用/测试引用）；②删 `chat_service` 无用 import 与模块 docstring "阶段1 过渡/Batch3" 过时叙述（改为全面 loop + 后台图谱分析描述）；③`llm_client` 头 docstring"接入阿里云千问"→"OpenAI 兼容（默认 MiniMax-M3）"、注释行"默认阿里云百炼"同步修正、import 去 AsyncGenerator；④`chat_service:399` 注释 call_llm→run_agent_loop；⑤AGENTS.md 同步：架构图/§3.3 表删 call_llm_stream 行、§3.6-9 过时注释条目删除、§3.6-3 上帝模块描述行数与"两代 chat"更新。方式：小改动用 replace_in_file，大块删函数因 docstring 尾随空格用一次性临时脚本按标记定位（backend/scripts/tmp_*.py 已删）。验证：lint 0、import 冒烟 OK、test_agent_loop+test_agent_run_store 20 passed。残余无关项：collector/store.py SQL 注释 "Batch3"（预留字段，另一主题）。后续更大债（按体检优先级）：①上下文窗口守卫与压缩 ②模型回退链（Votek quota-exhausted 经验）③llm_client 上帝模块拆分。
- **TODO 双文档盘点清理（2026-09-08 晚）**：按代码实测校准 TODO.md + TODO_Collector.md 过时/残留描述。TODO.md 主要改：头部日期 9/5→9/8 + 三赛同投定位；评估结论表刷新；P0 拆「对话/Agent 主链路真网验证 ✅」与「真实向量链路（等阿里 embedding）」与「quiz 判分联调」；P1 MiniMax 条目删 call_llm_stream 引用；P2 RAG 精排 W2→W4；归档区"两阶段流式"加注已统一收敛 run_agent_loop。TODO_Collector.md：B1.2"联调未做"→已验；Batch1 待验清单依赖 qwen-plus→MiniMax-M3；wikitext_to_md 维基地区词标记 `-{zh-cn:...;zh-tw:...;}-` 未清理登记为遗留债 #1；原「待确认」两条归档。
- **CI + 测试 + 失效脚本三小债已修（2026-09-08 晚，用户确认 OK）**：① `.github/workflows/backend-tests.yml` run 加 `-m "not llm_api"`（排除 real_api，需真 key + pytest-asyncio/pytest-timeout，pytest.ini 已有 marker）；② **token_counter 根因 bug 修复**：退化路径（无 tiktoken）对空 content 也 `max(1,0//3)=1`，与 tiktoken 路径空串=0 不一致 → `test_count_messages_tokens_none_content` 本地得 8 非 7；改为 `max(1, len//3) if content else 0`（与 count_tokens 空文本=0 语义对齐）。根因其实是 **venv 未装 tiktoken**（requirements 声明了但环境没装）暴露的代码不一致；③ 删除 `backend/test_data.py` 遗留失效脚本（KnowledgeGraph() 缺 user_id，零引用）。**验证：CI 等价命令 `pytest tests -m "not llm_api"` = 482 passed 全绿（7 deselected）**；lint 0。TODO.md 同步：评估表测试 482、运维 CI A、P1 CI 条 [x] 含修复说明、进度速览 482、遗留债 test_data 已删。注意 tests 目录 9/8 另有并行会话新增 `test_llm_fallback.py`（模型回退链 _chat_create/_LLM_CANDIDATES）+ `test_context_guard.py`（context_guard.py）等，本次全绿已覆盖。

## 上下文窗口守卫落地（体检优先级 ①，2026-09-08）
- 先调研同级 mutil_agent（software-dev-fullflow/src/loop/context/：budget 分区 critical/support 70/30、70%/85% 双阈值、compact_history 丢最旧、trim 保头尾、工具结果>2000 字符卸载外文件、字符级启发式估算无 tiktoken）。**结论：有框架没落地**——其真实客户端（llm-chat/minimax-chat/workbuddy_client）全量透传不守卫，context 包主要被 mock/单测驱动；无 LLM 摘要（docstring 自标"生产可换 LLM 摘要"）。借鉴其思想但用我们更强底座（真 tiktoken o200k_base+模型校准），不做 critical/support 分区（多 agent 多阶段产物，单教学 loop 过度设计）。
- 用户拍板参数：**预算 32K tokens**（成本目标非溢出防护——已核实 MiniMax-M3 官方上下文 1M；32K 与 mutil_agent 默认对齐）、**只丢最旧 + 插入省略说明**（不做 LLM 摘要，理由：被裁内容已沉淀图谱/画像/agent_runs，system_prompt 每轮重注入）。
- 落地：新增 `backend/app/core/context_guard.py`（`trim_history_to_budget(system_prompt, messages, *, budget, model, out_reserve=2000)` → (messages, info|None)）：本地预计数复用 `count_messages_tokens`（与 agent_loop estimated_prompt_tokens 同口径）；二分求最大可裁条数（fits 单调，~log n 次计数）；保证裁后以 user 开头 + 保留末条当前问题；头部插入 user 角色省略说明告知模型"早期历史已归档"（短文案，不打断教学）。config.py 新增 `llm_ctx_budget=32_000`（env LLM_CTX_BUDGET）。接入 chat_service.process_message 与 process_message_stream 的 run_agent_loop 前（各 1 行调用）。双形：dict/ChatMessage 混合返回 dict 列表（_build_api_messages 双形兼容，下游无 Pydantic 依赖）。软目标：system 自身超预算时不强裁照发（1M 窗口兜底）。
- 验证：test_context_guard.py 5 passed（未超原样同对象/丢最旧保末条+notice+计数下降/双形/空历史/单条超大不崩）；相关回归 test_agent_loop+test_token_counter+test_token_estimator 60 passed；唯一挂仍是既有 token_counter none_content（8vs7 与本次无关）。lint 0。
- 后续（未做，YAGNI）：LLM 摘要压缩（context_guard docstring 已标升级路径）、按用户可调预算 UI/运行时熔断、每轮 tool 回填增量不守卫（loop 内是必要思维链）。

## 模型回退链落地（体检优先级 ②，2026-09-08）
- 用户决策：备用资源"先搭框架等额度"（.env 留空档，阿里额度恢复/新 key 到位再激活）；前端**静默回退**（不推事件不打扰），并要求在 README 记下这层设计防遗忘。
- 现状核对：全 backend 真打 LLM 仅 2 处——`agent_loop._chat_once`（对话主链路）与 `llm_client.call_llm`（一次性文本），均走 `_with_retry(client.chat.completions.create)`；同模型重试对 402/403 配额耗尽无效（非 retryable 直接抛）。
- 落地（全部收敛在 llm_client，两调用点各 1 行）：config.py +3 可选字段 `fallback_llm_api_key/base_url/model_name`（env `FALLBACK_*`，缺任一不启用，key 缺省回退 DASHSCOPE_API_KEY 与主 client 对称）；llm_client.py 模块级 `_fallback_client`/`_LLM_CANDIDATES`（主→备用）+ `_should_fallback(e)`（触发：AuthenticationError、APIStatusError 401/402/403/429/5xx、超时/断连；**不触发 400/404/422 请求侧错误**）+ 公共 `async def _chat_create(**kwargs)`（按候选注入 model + _with_retry + 静默降级只 warning 日志 + 全败抛 _map_api_error 后 RuntimeError）。`call_llm` 去掉 try/except 直接走 _chat_create；`agent_loop._chat_once` 改用 _chat_create（import 清理 client/_with_retry/_map_api_error，MODEL_NAME 保留用于 token 估算）。
- 单级 fallback 是 YAGNI 边界（ponytail 注释：多供应商链=把 _LLM_CANDIDATES 加长）；token 估算仍按主模型（备用 tokenizer 差异忽略，ponytail 注释于设计段）。
- 文档：`.env.example` 加 FALLBACK 注释块（含"选不同供应商独立账号才有真冗余"提醒）；根 README 配置章环境变量表 +3 行（记忆点）。
- 验证：新增 `test_llm_fallback.py` 3 例（403 降级成功且主只打 1 次 / 单候选 403 照旧抛 / 400 不降级不碰备用）全绿；修 `test_minimax_thinking.py::test_extra_body_reasoning_split_passed`（原 patch `agent_loop._with_retry` 随 import 清理消失 → 改 patch `agent_loop._chat_create` 抓 kwargs）。相关 107 passed；全量 481 passed + 8 failed（7 = real_api 需 pytest-asyncio 插件环境问题、1 = token_counter none_content 既有 tiktoken 偏差，均非本次）。

## llm_client 上帝模块全量拆分（体检优先级 ③，2026-09-08 晚，用户拍板"全部拆出来"）
- **布局**：删除 `llm_client.py`（718 行），按职责拆为 9 个文件。`core/llm/` 原语包 = `__init__.py`（re-export 契约）+ `clients.py`（client/embed_client/MODEL_NAME/fallback_client 单例）+ `thinking.py`（LLM_EXTRA_BODY/strip_think_tags，MiniMax 适配）+ `messages.py`（build_api_messages）+ `retry.py`（is_retryable/with_retry/map_api_error）+ `fallback.py`（LLM_CANDIDATES/should_fallback/chat_create）+ `call.py`（call_llm）。工具侧 = `core/agent_tools.py`（8 个 spec+薄壳 handler+KG_TOOLS/execute_kg_tool 注册表分发）+ `core/rag_tool.py`（rag_search+_run_async+线程池）+ `core/web_tool.py`（fetch_webpage+SSRF+HTML→文本）。依赖单向无环：clients→(fallback←retry)←call；agent_tools→rag_tool/web_tool（薄壳）。
- **引用面全更新（16 处）**：agent_loop 改用别名保持命名零行为变化（`chat_create as _chat_create`、`build_api_messages as _build_api_messages`、`strip_think_tags as _strip_think_tags`、`LLM_EXTRA_BODY as _LLM_EXTRA_BODY`、MODEL_NAME 直 import，KG_TOOLS/execute_kg_tool 改自 agent_tools）；quiz/generator、quiz/grader、kb/graph_generator、graph_analyzer 的 call_llm 改 `from app.core.llm import call_llm`；rag/manager、kb/kb_manager 的 `embed_client as client` 同源。测试 3 文件同步（test_llm_fallback 改 `from app.core.llm import fallback as llm_client` + 公开名 LLM_CANDIDATES/chat_create；test_minimax_thinking import strip_think_tags；test_rag_tool 按模块分流）。
- **AGENTS.md §3.6-1/2/3 债状态同步**：私有符号跨模块 ✅ 解决（收敛为 llm 包公开契约）；上帝模块 ✅ 解决（718 行归零，全库零 llm_client 残留）；core→api 反向依赖保留（agent_tools._h_add_node→knowledge.create_node_from_ai，另作下沉）；同步↔异步两层线程池仍在（_run_async 随 rag_tool 私有）；测试命令行加 `-m "not llm_api"`。
- **验证**：受影响 5 测试文件 38 passed；全量 `pytest tests -m "not llm_api"` = **482 passed 全绿（7 deselected = real_api 付费测试，既有环境需 pytest-asyncio）**；`import app.main` + 全符号冒烟 OK（KG_TOOLS 8 工具、LLM_CANDIDATES 候选与 MODEL_NAME 一致）；lint 0。
- 后续（未做）：create_node_from_ai 下沉 core（§3.6-1 另一半）、agent_loop 上下文管理独立文件、前端可视化第二步。

## agent_loop 上下文管理独立文件（agent 运行三分法补齐，2026-09-08 晚）
- 新建 `core/agent_context.py`（agent 运行三分法：**上下文缓冲 agent_context.py / 发送前守卫 context_guard.py / 事件分发 agent_events.py** 各自独立）。`AgentContext`（`__init__` 即 build_api_messages 双形组装）+ 模块级纯函数 `assistant_snapshot(msg)`（含 reasoning_details 思维链保留，原 agent_loop._assistant_snapshot 搬来）+ 4 个 append 方法：`append_assistant_turn`（带 tool_calls 快照）/`append_assistant_text`/`append_tool_result`（按 tool_call_id 配对）/`append_user`。协议约束（快照/思考块保留/tool 配对）收敛单点，agent_loop 不再直接 append dict。
- agent_loop.py：`_loop_core` 改为 `ctx = AgentContext(system_prompt, messages)`，三处消息写入换 ctx 方法、`_force_finish` 参数由 api_messages 改传 ctx；删 `_assistant_snapshot` 函数定义与 datetime/Path 旧导出兼容 import（全库零引用）；`_chat_once(api_messages)` 参数名保留（指单次调用输入，非上下文职责）。
- 测试：test_minimax_thinking.py 2 处 `agent_loop._assistant_snapshot` 改 `from app.core.agent_context import assistant_snapshot`。验证：相关 5 文件 40 passed；全量 482 passed + 7 deselected（既有）；import+冒烟 OK（4 消息含快照+tool 配对）；lint 0。AGENTS.md §1.1 目录速览补 agent_context/agent_events/context_guard 三行。
- 说明：Batch2「messages 窗口裁剪」（docs/AgentLoop_重构设计讨论.md §②简单窗口化保头尾）的 loop 内落点已由 agent_context docstring 标注（append 之前做守卫）；入口一次性守卫 context_guard 已生效，loop 内每轮 tool 回填属必要思维链刻意不守卫。
