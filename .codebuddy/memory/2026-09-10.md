# 2026-09-10

## 完成：节点内容 Markdown 分屏编辑（TODO P1 最后一条的纯代码部分）

**背景**：用户要求「浅浅做一个模块」。看 TODO 后确认 P0 残余项全卡在外部资源（阿里 embedding 额度 / 真网 / 演示数据），唯一零依赖纯代码项是 P1 最后的「text→Markdown 分屏编辑」。

**改动（仅 1 个文件）**：`frontend/src/components/NodeDetail.vue`
- script：新增 `previewHtml = computed(() => renderMarkdown(editContent.value))`，与阅读模式 `htmlContent` 共用 `utils/markdown.js` 渲染管线（marked+KaTeX+hljs+DOMPurify），未新增任何依赖/组件文件。
- template：编辑模式 `.edit-area` 内由单 textarea 改为 `textarea` + `div.markdown-body.edit-preview[v-html=previewHtml]` 双栏；saveError 的 `<p>` 移入 `.edit-area` 内部（**踩坑**：一度插在 `v-if="mode==='edit'"` 与 `v-else-if="mode==='view'"` 之间，会打断条件分支配对，已改回内部并用 grid-column 铺满）。
- CSS：`.edit-area` 改 `display:grid; grid-template-columns:1fr 1fr; gap:12px`；新增 `.edit-preview`（边框/圆角/独立滚动/复用 `.markdown-body` 排版）；`@media (max-width:760px)` 单列堆叠；`.save-error` 加 `grid-column: 1/-1`。

**验证**：`npm run build`（frontend）✓ built in 8.83s，2288 modules transformed，无报错；read_lints 0 诊断。

**刻意未做**（YAGNI，需要时再加）：
- 未同步 textarea 与预览的滚动位置；
- 未给预览渲染加 debounce/防抖（短文档无感）；
- 未抽独立 `MarkdownEditor.vue` 组件（当前只有 NodeDetail 一处使用；EditDialog 的「创建节点初始内容」将来若也要分屏再抽）；
- 未动后端提示词层（「教学后主动记笔记」子项仍待做）。

**TODO.md 同步**：P1 那行拆为两条（分屏编辑已勾选 + 笔记提示词未做），头部「今日」日期更新为 2026-09-10（W3 第 3 天）。
