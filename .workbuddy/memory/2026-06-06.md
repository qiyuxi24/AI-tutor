# 2026-06-06

## 登录逻辑 & 数据库审查 + 修复

### 审查结论
- 项目架构：FastAPI 后端 + Vue 3 前端 + SQLite，登录流程完整（bcrypt + JWT 24h）
- 数据库（data/knowledge/knowledge.db）可正常访问，当前 5 个用户
- SQL 注入无风险（全参数化查询）

### 修复内容
1. **backend/.env** — 添加强随机 SECRET_KEY（64 位 hex）
2. **backend/app/core/auth.py:22** — 去掉弱默认值 `"default-secret-change-me"`，未配置则 raise RuntimeError 拒绝启动
3. **frontend/src/views/LoginView.vue:64** — 密码最小长度从 4 改为 6，与后端保持一致

### 遗留提醒
- 限流器为进程内存存储，生产多 Worker 部署时建议改 Redis
- CORS 白名单 `localhost:5173` 正式部署需更新为生产域名
- 确认 backend/.env 是否已被 git 追踪（如有需轮转 DashScope API Key）
