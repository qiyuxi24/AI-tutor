# ════════════════════════════════════════════
# 环境变量 & 密钥 — 绝对不能上传
# ════════════════════════════════════════════
.env
*.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
secrets/
credentials.json

# ════════════════════════════════════════════
# Python
# ════════════════════════════════════════════
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg
.eggs/
venv/
.venv/
env/
*.db
*.sqlite3

# ════════════════════════════════════════════
# Node / Frontend
# ════════════════════════════════════════════
node_modules/
dist/
.vite/
*.local

# ════════════════════════════════════════════
# IDE / 编辑器
# ════════════════════════════════════════════
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ════════════════════════════════════════════
# 日志 & 临时文件
# ════════════════════════════════════════════
*.log
logs/
tmp/
temp/
*.tmp
.cache/

# ════════════════════════════════════════════
# AI / 项目数据（按需调整）
# ════════════════════════════════════════════
data/ai_suggestions.json

# RAG 评测临时索引（可每次重建；评测集 cmrc2018_trial.json 保留）
backend/eval_data/kb_eval/
backend/eval_data/prof_test/
backend/eval_data/prof_sparse/
backend/eval_data/prof_writer/

# ════════════════════════════════════════════
# AI 编程助手 / 自动化工具产物
# .codebuddy 与 .workbuddy 的工作记忆、Agent 配置全队共享；
# 仅排除第三方插件、本机配置与个人日志
# ════════════════════════════════════════════
.trae/

.playwright-cli/
# Playwright 页面快照（根目录临时文件）
/init.yaml
/login.yaml
# 第三方插件库（ponytail 等，成员按需自行安装）
.codebuddy/skills/
# 本机插件配置
.codebuddy/settings.local.json
# 个人求职日志（与项目无关，勿提交）
.workbuddy/memory/2026-09-01.md
# PPT 生成工具的工作目录：进程状态（.slidep/）、state.json、图标缓存等可重建产物。
# 成品 PPT 若要留档，请拷到 docs/ 或根目录再提交，不要从工具目录里提交。
/.workbuddy/ppt_workspace/

# ════════════════════════════════════════════
# 运行时数据 / 生成文件
# ════════════════════════════════════════════
# 备份文件
*.bak
# SQLite 运行时文件（WAL / 共享内存）
*.db-shm
*.db-wal
# Whoosh 检索索引（可由知识库重建）
whoosh_index/
# 后端运行时数据（知识库索引、测验库等，可重建）
backend/data/

# 项目根 data/ 运行时数据（用户图谱 / 画像 / trace / KB 索引等，本地生成不入库）
# 注意：data/prompts/*.j2 模板与 data/ 下 *.md 文档仍保留入库
/data/knowledge/
/data/profiles/
/data/traces/
/data/conversations/
/data/quiz/
/data/kb/
/data/rag/
/data/collector/

# 一次性比赛/调研材料（本地保留，不入库）
/guochuang.pdf
/guochuang*.txt
/*.PDF
# docs/比赛/官方材料/：官方通知与一手材料（PDF/文本），本地保留、不入库
/docs/比赛/官方材料/

# ════════════════════════════════════════════
# 外部调研仓库（对标/借鉴用，clone 下来的第三方源码）
# ════════════════════════════════════════════
.research/