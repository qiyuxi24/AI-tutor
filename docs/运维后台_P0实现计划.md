# 运维后台 P0 实现计划

## 目标
创建一个完全独立的运维管理后台，与主系统解耦。

## 架构设计

```
┌─────────────────┐    ┌─────────────────┐
│   主系统前端     │    │  运维后台前端   │
│  localhost:5173 │    │  localhost:5174 │
└────────┬────────┘    └────────┬────────┘
         │                       │
         │               ┌───────┴───────┐
         │               │  运维后台后端  │
         │               │  localhost:8001│
         │               └───────┬───────┘
┌────────┴────────┐              │
│   主系统后端     │              │
│  localhost:8000 │              │
└────────┬────────┘              │
         │                       │
         └───────┬───────────────┘
                 │
         ┌───────┴───────┐
         │   共享数据库   │
         └───────────────┘
```

## 服务规划

| 服务 | 端口 | 说明 |
|------|------|------|
| 主系统后端 | 8000 | 现有 |
| 主系统前端 | 5173 | 现有 |
| 运维后端 | **8001** | 新建，仅管理员访问 |
| 运维前端 | **5174** | 新建，专用管理界面 |

## P0 功能清单

### 1. 用户列表
- 展示所有用户（分页）
- 搜索（按用户名/邮箱）
- 筛选（按状态：正常/禁用）

### 2. 用户详情
- 查看用户资料
- 修改用户角色（普通用户/管理员）
- 禁用/启用用户

### 3. 密码重置
- 管理员重置用户密码

## 技术方案

### 后端 (FastAPI + 原生 sqlite3，无 ORM)
```
backend-admin/
├── app/
│   ├── main.py                  # 入口 + lifespan（建表 / 初始化超管）
│   ├── api/v1/admin/
│   │   ├── __init__.py          # 路由汇总
│   │   ├── auth.py              # 登录 / me
│   │   ├── users.py             # 列表/详情/禁用/启用/重置密码/改角色
│   │   └── audit_logs.py        # 审计日志查询
│   └── core/
│       ├── config.py            # dataclass + os.getenv（沿用主系统约定）
│       ├── db.py                # 连接依赖 + 审计日志写入 + 建表
│       └── admin_auth.py        # bcrypt + JWT
├── tests/test_admin_api.py      # 13 项 API 测试
├── conftest.py
└── requirements.txt             # 版本与主系统对齐
```

约定（与主系统保持一致）：
- 密码哈希用 `bcrypt`（同 `backend/app/core/auth.py`）
- 不引入 pydantic-settings / SQLAlchemy，配置用 dataclass + os.getenv
- 业务更新与审计日志在同一事务内提交

### 前端 (Vue 3 + Element Plus + Vite)
```
frontend-admin/
├── src/
│   ├── api.js              # 统一 axios 实例：自动带 token，401 回登录页
│   ├── router.js
│   ├── views/
│   │   ├── Login.vue
│   │   ├── Layout.vue      # 侧边栏布局
│   │   ├── Users.vue       # 用户管理
│   │   └── AuditLogs.vue   # 审计日志
│   └── main.js
└── vite.config.js          # /api → http://localhost:8001
```

## 测试

```powershell
cd backend-admin
..\backend\venv\Scripts\python.exe -m pytest tests -q    # 13 passed

cd ..\frontend-admin
npm run build                                            # 构建通过
```

测试用临时 sqlite 文件冒充主系统库（`users` 表），不会触碰真实的
`data/knowledge/knowledge.db`。

## 启动脚本

`start.ps1` 已集成运维后台（端口 8001 / 5174），会按需创建
`backend-admin/venv`、安装 `frontend-admin/node_modules`，并在退出时一并清理。

## 主系统旧实现已移除

避免两套 API 写同一张表：
- 删除 `backend/app/api/v1/admin.py` 及其路由注册
- 删除 `frontend/src/views/AdminView.vue`、`/admin` 路由、ActivityBar 账号管理菜单项

## 实现顺序

1. 创建 `backend-admin`，复用主系统 SQLite
2. 用户管理 API（列表/详情/禁用/启用/重置密码/改角色）
3. 审计日志写入 + 查询
4. `frontend-admin` 界面（登录/用户管理/审计日志）
5. `start.ps1` 集成
6. 自动化测试 + 端到端验证

## 待确认

- [x] 管理员账号：新建专用 admin 账号
- [x] 审计日志：记录管理员操作

## 管理员账号设计

### 表结构 (admin_users)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| username | VARCHAR(50) | 登录名（唯一） |
| password_hash | VARCHAR(255) | 密码 |
| role | ENUM | super_admin / admin |
| created_at | DATETIME | 创建时间 |
| last_login_at | DATETIME | 最后登录时间 |

### 审计日志表 (admin_audit_logs)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT (UUID) | 主键 |
| admin_id | TEXT | 管理员 ID |
| admin_username | TEXT | 操作人（冗余，避免关联查询） |
| action | TEXT | login / reset_password / disable_user / enable_user / update_role |
| target_user_id | TEXT | 目标用户 ID |
| target_username | TEXT | 目标用户名 |
| ip_address | TEXT | 来源 IP（取自请求，非写死） |
| details | TEXT | 变更详情，如「角色 user → admin」 |
| created_at | TEXT | 操作时间（ISO8601, UTC） |

### 初始化
- 初始超级管理员：`admin` / `admin123`（仅首次初始化，可用 `ADMIN_DEFAULT_PASSWORD` 覆盖）
- 运维后台使用独立的 `ADMIN_SECRET_KEY`，与主系统 token 互不通用
